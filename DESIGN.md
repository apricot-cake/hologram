# Corpus デザイン言語

ビューア（`app/renderer/`）UI変更時の規範。トークン実体＝[`design-tokens.css`](app/renderer/design-tokens.css)（コンポーネントは semantic 変数のみ・primitive 直書き禁止）。

## 原則

- 新しい形・色・動きを増やす前に、既存の語彙で表現できないか必ず先に考える。
- 新UIパターンは必ず実プロダクト（Apple HIG / Fluent / macOS / iOS / Finder / Notion / Linear 等）に錨を下ろす。独自の感性で発明しない。提案時は「どのプロダクトのどのパターンか」を添え、採用したらこのファイルに前例ごと記録。

## 基礎

### エトス

Corpus は「企業的無機質さ」への対抗＝個人が作った・あなたのもの・とっつきやすい道具。出発点は「これ以上プラットフォームに自由を奪われたくない」（→オフライン所有）＋「難解なソフトはもう使いたくない」。

- **姿勢の前例＝Misskey**（非企業的・囲い込まれない）。借りるのは"姿勢"であって"様式"ではない＝倒し方は Misskey の表現過剰でなく Corpus の**静かなクラフトと抑制**（モノクロ＋ガラス／透明箱を作らない／アクセント1点／モーション最小）。
- **表現力は手放さず層にする（低い床／高い天井）**: パワー（クエリビルダー等）は全部残すが、最初に触る面の単純さを死守。深さは"消す"のでなく"潜れる所に置く"。前例＝Notion／Things／Misskey。
- **運用の問い**: 機能ごとに「床（初手で出会う）か天井（潜って届く）か」を問い、既定の面に出すのは床だけ。基準は「強力か」でなく「隠してもなお背負う概念的重さに見合うか」。

### ブランド／ロゴ（2026-06-28 改定）

- **ロゴ＝ホログラフィック虹色スクエア**（ミント→水色→ラベンダー→ピンク→ピーチのイリデセント・角丸/影/装飾なしの素スクエア）。面主体で 16/32px でも色面として読める。生成元＝Claude Design シェーダー（`about-icon.js` に WebGL 移植）。**ラスターが実体**（ベクター化不可）・マスター＝`assets/icon-master.png`。
  - 旧 `{ ♡ }` は 2026-06-28 退役（16/32px で潰れる問題を面主体スクエアで解消＝小サイズ視認性と独自性を意味性より優先）。**再提案しない**。
- **ワードマーク＝小文字 `corpus`（IBM Plex Mono Medium）**。バナーSVGは**アウトライン化**（GitHubでwebフォント不可・`assets/banner-*.svg`）。ロゴ刷新後もワードマーク継続（マークだけ差替）。
- **色**: スクエアは固定イリデセントで light/dark 共通。UIの水色アクセント（`--sky-*`）はロゴと別レイヤーで不変。
- **アセット再生成（単一導線）**: `assets/icon-master.png` 差替→`electron scripts/make-icons.js` 一回で全アイコン再生成（Electron 512／Chrome拡張 16-256／汎用256／バナー4種）。配置先が増えたら `make-icons.js` の `TARGETS`/`BANNERS` に足す。手順＝[docs/build.md](docs/build.md)。
- **表記＝二段構え**（普通名詞 `corpus` の固有名詞性を守る）: ロゴ/リポ名/CLI/技術文脈→小文字 `corpus`／OS表示名/`productName`/タイトルバー/README散文→`Corpus`。前例＝amazon・spotify。全小文字統一は埋もれるので不採用。
- **コピー**: タグライン「投稿を丸ごと。あなたの SNS ライブラリに。」訴求軸は**「投稿」**（画像は投稿の部分集合）。

### 用語

- **投稿した人＝「投稿者」**（2026-06-15・イラスト以外も保存するので「作者」不採用）。**@ハンドル＝「ユーザー名」**として別概念で残す（投稿者＝表示名／ユーザー名＝@handle・EN: Poster/Username）。「ユーザー」を人の意味で使わない。
- **2軸の呼称（2026-06-22）＝〈何を見るか〉「ビュー」×〈どう並べるか〉「レイアウト」**。ビュー＝投稿/投稿者/コレクション。レイアウト＝カード/タイル/リスト。"閲覧/表示"は語感がかぶるため廃止。
  - ビュー切替・レイアウト切替にも**セクション見出し**（`.sb-title`）を付ける＝検索/並び順と統一。トグル内ラベルは具体物のまま。
  - 投稿ビューのUIラベル＝「ライブラリ」（EN: Library/Posters）・会話では「投稿ビュー」。識別子（`browseMode` 等）は原語。
- **ファセット件数（2026-06-26）**＝フィルタフライアウト各値に「現クエリで絞った場合の件数」を出す（標準語 facet counts に準拠）。件数降順・0件は末尾に淡色。UI部品呼称＝「件数バッジ」（実装 `facetCounts`）。

### 声と語り口（2026-06-20）

- **原則: ラベルとマイクロコピーは「何をするか／何が起きるか」を日常語で言う**。抽象モード名（「通常」）やジャーゴンで済ませない。
- **温度＝平易で温かい（friendly-plain）**。スラング・ノリ・あざとさは避ける（流行り言葉は寿命が短い）。前例＝Mailchimp/Things/Linear/37signals。
- **破壊的操作には必ず安心を括弧で添える**: 「すべて削除しますか？（中の画像自体は消えません）」式。何が消えて何が残るかを聞く前に言う（`wsEmptyConfirm` 等に既出）。
- **砕けた語は短い補足（ヒント/title）で正確さを1行担保**。例＝「おおまか」＋「タイプミスや言い回しの違いも拾う」。
- **1概念1語**（同じものを2語で呼ばない）。**ドメイン語・識別子は原語のまま**（リポスト等訳さない）。
- **第一適用＝検索モード**: 「通常/あいまい」→**「ぴったり／おおまか」**（EN: Exact/Loose）。UI＝両方常に見せる macOS/iOS スコープバー式セグメント（`.seg-control`・凹トラック＋浮きつまみ）＋選んだ動作を一行補足（`.seg-hint`）。旧・右端単独チップ（発見性低）を置換。フィルタ版＝`.seg-control--qf`（ヒントはツールチップ）。

## ビジュアル言語

### 形の言語（シェイプ＝意味）

| 形 | 意味 | 例 |
|---|---|---|
| 丸ピル（`--radius-pill`） | **値**（フィルタ値・トグル対象） | タグ/投稿者/PFチップ、クエリビルダーピル |
| 角丸四角（6–8px） | **場所・操作**（押すと開く/起こる） | サイドバー行、CTA、各種ボタン、タブ |
| ○リング（塗りなし） | **選択** | カード左上の選択リング |

この対応は崩さない（「値なのに四角」「ナビなのにピル」を作らない）。

- **サイドバーのカテゴリ行＝枠なしテキスト行**（静止時は背景透明・枠なし、ホバー時のみ `--hover` 塗り＝Finder/Notion式）。全行を塗り＋枠線の箱にするのは**却下**（Word図形/ワイヤーフレーム感）。枠線は入力欄（検索・select）にだけ許す。
- **カテゴリ行に行頭アイコン**（14px・lucide系モノトーン・`--text-muted`）。行ホバーでアイコンも `--text` に持ち上げ。
- **シェブロン＝くの字SVG**（細線・塗りなし）。▸/▾ の塗り三角は不使用。全展開インジケータを `.sb-row-arrow, .sb-subrow-arrow, .cs-arrow` の1ブロックで管理（`--text-muted`・11px・`rotate(-90deg)` で方向切替）。追加時はこのブロックに追記。
- **「枠だけの透明箱」を作らない＝形は塗りで作る**:
  - 値チップ＝**塗りピル・枠なし**（GitHub topics/Notion）。塗りは背景から明確に1段離す（`--chip-bg`/`--chip-bg-hover`。`--surface-3` は差5%でノイズ化＝**却下**）。アクティブ tint の縁だけ枠を持つ。
  - セグメンテッドコントロール＝**凹トラック＋浮きつまみ**（macOS式・外枠/仕切りなし）。レイアウト切替はアイコングリフ＝非アクティブはアウトライン（`fill:none`）・アクティブは塗り＋テキストラベル（iOS/macOSタブバー）。ツールチップ付けない。非アクティブのホバーはグリフ明度だけ上げる。
  - タブ帯（`#tabBar`）＝**常設面なのでガラス禁止**。タブは角丸四角（場所の形・ピル禁止）、アクティブは浮きつまみ（tint/ソリッド不使用）、非アクティブは透明・`--text-muted`。前例＝Chrome/Arc（構造）＋macOSセグメント（アクティブ）。
  - アイコンボタン＝**ゴースト**（素グリフ・ホバーで `--hover`）。
  - 入力欄＝**凹んだ塗り（`--field-bg`）＋見える縁（`--border-strong`）**＝macOS検索フィールド解剖。`--border-subtle` の極細縁は埋もれて**却下**。透明箱が許されるのは focus リングとアクティブ tint のみ。
- **プルダウン＝カスタムグラスドロップダウン**: native `<select>` はOS描画でガラス化不可のため `display:none`（値保持・changeハンドラは生きる）＋フィールド体裁トリガー（`.cs-btn`）＋グラス一覧（`.cs-pop`・`.fold-menu` 流用）で置換（`enhanceSelect()`）。外部変更時は `refreshCustomSelects()` で同期。対象＝並び順・検索方式（設定モーダル内は対象外）。

### 色

- ベース＝モノトーン（cool slate）＋**水色（sky）アクセント**（2026-06-18 インディゴから変更）。アクセントは2ステップ: `--accent`（濃＝白文字/✓が乗る塗り・色文字。light/dark とも `--sky-600`）と `--accent-soft`（淡＝上に何も乗らない輪郭・選択リング。`--sky-300`系）。前例＝Linear/Stripe。**注意**: 水色は明るく白文字コントラストが旧インディゴより低い（~3:1）＝弱ければ塗りだけ一段濃く（色相維持）。**2026-07-02 発動**: dark（旧 sky-500）の白文字が 2.72 と contrast-parity のアイコン床 3.0 未達→sky-600 へ一段（両テーマ 3.32・テスト床はアイコン/大文字ティア 3.0）。
- **アクセントは「機能的に意味のある一点」だけ**（選択リング・選択チェック・primary・focus・リンク）。フィルタ値/タグ/WS/ビルダーの**アクティブ表現はモノトーンのコントラスト**（`--accent-subtle` 系をグレーへ再定義済み＝淡グレー面＋`--text-strong`）。前例＝Linear/Vercel/Notion/Things。**ソリッド塗りのアクティブも禁止**（許されるのは danger と画面唯一の真の primary だけ）。
- **カテゴリ色分け（`--tint-*`）はアクセントと別レイヤーで残す**（プラットフォーム/タグ等の分類色）。
- **ホバー＝ニュートラル（モノトーン薄塗り）、アクセント＝選択/アクティブの状態**。ホバーにアクセント色を使うとにじむ＋状態表示の意味が薄まる（例外＝取り消し予告の赤）。
- 破壊操作＝`--danger`。**赤の color-mix は srgb**（oklch はダーク背景で紫に転ぶ）。
- アイコン＝モノトーンSVG（`currentColor`）。絵文字/固有色アイコンを増やさない。

### ガラス（フロスト／レンズ）

**基準＝面の分類「浮遊面」か「接地面」か**（2026-06-14・根拠 Fluent Acrylic/Mica ＋ Apple HIG）。

- **浮遊面（画面上に浮く）＝すべてガラス可**: ポップ/右クリックメニュー/フライアウト/日付・エンゲージポップ/モーダル/トースト/stickyバー/浮遊時インスペクタ/ライトボックス暗幕/カードホバーボタン/プルダウン/最上部に戻る。背後にコンテンツが流れれば最も映えるが、単色 Mica 上でもガラス可。
- **接地面（レイアウト固定）＝Mica 材質**: サイドバー・カード・インライン時インスペクタ。ガラスだと「濁ったグレー」＋常時GPU負荷のため Mica（薄い縦グラデ＋微細ノイズ `feTurbulence`＋上端1pxハイライト）。**接地面でも「動き/選択の瞬間」だけガラス化は可**（Apple Liquid Glass＝インタラクションで現れるレンズ）。

**3層システム＝「背後に何があるか」で選ぶ**（共通＝明るいリム＋斜めスペキュラ `::before`）:
- **A `.glass-lens`**（背後＝リッチコンテンツ）: 高透過(14%)＋薄ブラー(8px)＝**読み抜ける**。テキスト密なホストは厚く（`.kb-hint-pop` は 42%）。例＝ショートカット一覧。
- **B `.glass-frost`**（背後＝低情報）: 中不透明(55%)＋強ブラー(18px)＝busy背景に依存しない。例＝プルダウン `.cs-pop`。
- **C `.glass-relief`**（背後＝なし）: 内部焼込みの上明→下暗グラデ＋スペキュラ＋リム。backdrop-filter は効かない前提。例＝レイアウト切替 `.view-toggle`。
  - **トグルつまみ（`.vt-thumb`）＝フラットだがリッチなリキッドガラス**: ドーム反射/bulge は載せない（「膨らんだプラスチックビーズ」指摘 2026-06-21 を解消）。フラットのまま①屈折（`backdrop-filter: url(#glassRefract)`）②彩度上げ frost（コントラスト確保）③外周ヘアライン＋上端の光ライン（`inset 0 1px 0`）④柔らか落ち影。凸/凹の錯覚は避け、リッチさは屈折と縁の光で。
- **本物の屈折＝SVG変位フィルタ `#glassRefract`**（2026-06-16）: コンテンツ上に浮く面（A/B）は `backdrop-filter: url(#glassRefract)` で背後を**実際に歪ませる**（CSS blur は位置そのまま・これは波打つ）。適用＝`.fold-menu`/`.qf-popover`/`.inspector`(浮遊時)/`#sbTop`。**除外**＝設定モーダル（大面積・重い→blur据置）と `.glass-lens`（歪ませると読めない）。**歪みは控えめ厳守**（強すぎ＝生成AI感）。C には効かない。

**ハード制約（実験でも守る）**: ガラスを重ねない（濁る）／色収差はエッジ局所で控えめ／アクセント色ソリッドブロブをサムネ上に置かない（`.act-pill` はニュートラル白）／透明混合は srgb／`prefers-reduced-transparency` でソリッドに戻すエスケープ必須。

**リキッドホバー（`.act-pill`）**: 🔖↔ℹ 間をニュートラル白の膜が滑って追従（iPadOSポインタ式・`:has()` のみ・JSなし）。ピルはボタン上層(z2)に重ねる。`prefers-reduced-motion` ではフェードのみ。

### モーション

- 150–260ms / ease-out。出現は fade＋4–6px rise（`corpusFadeIn`/`corpusPopIn`）。
- 入場アニメは**「新規構築」時のみ**（初回ロード・フィルタ/検索/ソート/レイアウト切替）。**ミューテーション（追加解除・削除・グループ変更・タグ保存）とバックグラウンド更新（fs-watch・再読込）では再生しない**（keepLimit 再描画。「画面がリフレッシュした」感）。ページ送りも再生しない。
- `prefers-reduced-motion: reduce` で必ず無効化。

## レイアウトと余白

### 全体構造（2026-06-16）

- **クエリビルダー＝全幅の上部バー**（`#postActiveBar`/`#posterActiveBar`・tabBar直下・常時表示）。左端まで伸びサイドバー上を貫く。高さは JS が `--activebar-h` に実測反映。両ビューは同一実装（`createQueryBuilder` factory）を共有。前例＝Claudeデスクトップ。
- **サイドバー＝「ウィンドウ」として浮かせる**（角丸14px・四辺 `--sidebar-float`(10px)・枠＋`--shadow-md`）。sticky top はクエリバー＋余白分オフセット。前例＝Claudeデスクトップ。
- **背景グリッド（2026-06-18）**: `#appBody` に薄い方眼常時（`color-mix(--text 8%)`・1px・26pxセル・テーマ追従）。前例＝Figma/Miro。ガラスで屈折させる案は**不採用**（`#glassRefract` の blur で細線が消える）。

### 余白のリズム

- **サイドバー横軸は `--sb-gutter`（1変数）で一元管理**。`.sb-scroll` が inset を持ち各要素は左右パディングなし full-width＝箱の左右が構造的に揃う（旧 12/13/14px のバラ付き撤去）。軸を動かすときは gutter 1か所だけ。
  - **2軸**: セクション見出し（`.sb-title`）は padding 0 でガター（箱の左端）＝一段外。コンテンツは内側 1 content-pad(≈12px)＝一段内。見出し外・中身内で階層が読める。
  - サブセクション（`.sb-subtitle`）はタグの子＝小さく薄く（10px `--text-subtle`）＋インデント。
- 大セクションは**余白で分ける（26px）＝区切り線は使わない**（線より余白で構造）。
- **フィーチャー起動ボタン＝枠なし塗り（`--chip-bg`）＋アクセント色アイコン**。枠付きtint箱は**却下**。
- サイドバー横長コントロールは上下スリム（行/レイアウト切替=5px・select=5px・検索=6px の縦パディング）。ダイアログ既定(8px)を持ち込まない。
- グリッド側コンテンツ pad: 24px 32px。

## インタラクションと情報設計

### 操作語彙

- **クリック＝トグル**（追加/解除）。3状態サイクルは廃止済み＝**復活させない**。
- **クエリビルダー＝バー上のドラッグ式インライン・ビルダー**（改訂③・2026-06-19）: 条件はフラットに並びトップ階層＝かつ。演算子は連結語クリックでグループ単位トグル・ピル→ピルのドラッグで括弧グループ化・ホバー≠で否定・「グループで囲う」で一括ネスト。`queryTree` が単一真実源。詳細＝docs/design-query-builder.md「改訂③」。
- **ホバー＝最頻アクションだけ**（カードは🔖・ℹ・🏷の3個まで）。取り消し系は控えめな赤。**4個以上に増やすのは飽和の再発＝却下**。
- **右クリック＝全アクションの目次**（開く/タグ/フォルダ/WS/詳細/投稿者/削除）。低頻度・破壊的・「選ぶ」系はホバーでなくここ（fold-menu様式・破壊行は赤 `fm-danger`）。
- **フライアウト＝サイドバー行の右に開く**（カテゴリ→値）。8件超は絞り込み入力（プラットフォームは固定短リストなので無し）。日付・エンゲージ入力ポップも**右に開く**（真下は下の行を覆う）＝全ポップ右出しで統一。
- **モーダル＝設定・確認のみ**（中央＋backdrop）。全データ削除等はキーワード入力ゲート必須。
- **インスペクタ＝ℹで右カラム常設（非モーダル）**。狭幅(<1280px)は同パネルがスライドオーバーに（消えない・レイヤリングだけ変わる）。対象カードに tint リング。スライドオーバー時はパネル外クリックで閉じる（クリック消費）。投稿インスペクタの投稿者名/アバターはその投稿者ビューへのリンク（双方向ナビ）。
- **頻度ベースの自動表示UIは採用しない**（「よく使うタグ」等・認知負荷で却下）。
- **クリップ vs コレクション**＝「一時フラグ」と「永続の棚」:
  - **クリップ**＝カードのホバーボタン（📎 lucide paperclip）ワンクリックの一時印（単一・迷いゼロ）。**クリップ済み＝ボタンがアクセント色に反転**（GitHubスター/iOSいいね語彙・旧ベタ塗り＆ドット却下 2026-06-27）。ホバー赤（外す予告）は**廃止**（気軽なトグルに赤は大げさ）。サイドバー「クリップ」行はフィルタ群先頭。一括解除＝「空にする」＋確認（「クリア」はフィルタ解除と誤読で却下・投稿は消えない旨明記）。実体＝`collections.json` トップレベルの独立フラグ集合。
  - **コレクション（フォルダ）**＝複数・名前付き・右クリック→ピッカーで追加先選択。**デフォルトフォルダは廃止**（ワンクリック自動追加は「1つしか入れられない」誤解を生んだ）。旧「アクティブ(★)＝🔖投入先」も廃止。
- **コレクションビュー（第3ビュー）**＝一覧面。カード＝名前＋件数＋サムネ（件数別モザイク 1=全面/2=左右/3=大+2段/4=2×2）＋動的は⚡。クリック＝開く・右クリック＝目次・戻る＝常設トグル「コレクション」。**静的（手集め）と動的（保存検索）が同グリッド共存**（動的＝保存条件を今のライブラリに当てて件数/サムネ実時間算出＝開けば最新）。入口＝クエリ帯「検索を保存」（絞り込みがある時だけ・枠なし塗り＋アクセント色）。動的の右クリック＝「条件を今の絞り込みで更新」。
- **タグ付け＝2導線（モード/ツールバー/ウィザードは廃止）**: ①インスペクタでのカード編集（グループ別全タグをクリックで付け外し＝recognition over recall＋新規作成）。②複数選択→一括付与（クリック/Shift/Ctrl+A→選択バー「選択分にタグ」・足すだけで置換しない）。前例＝Finder/写真.app。**「ウィザード」等の開発者語彙をUIで使わない。レコメンド/自動候補は不採用**（決定的・ユーザー主導）。
- **ショートカット**: `Alt+S`（キャプチャ）/ `Ctrl+A`（全選択）/ `/`・`Ctrl+K`（検索フォーカス）。グローバルショートカットは「入力中・オーバーレイ中は奪わない」ガード必須（Ctrl+A が雛形）。

### カードの情報階層（Ivory/Tweetbot のセル解剖準拠）

- **1行目**: 投稿者名(600)＋@handle(subtle・省略)のみ。
- **フラグ**（リプライ/引用/スレッド/メディア種）: 淡色テキストのみ・チップにしない。
- **本文**: 3行クランプ（リストは2行）。
- **最終行（`.post-foot`）**: 左=非ゼロのエンゲージのみ、右=**出典コーナー（`.foot-r`）**＝PFラベル＋コンパクト日付（同年=M/D）。
  - **PFラベル（`.pf-tag`）＝ブランドドット7px＋mutedキャップス10px**・フッター右（Reederのソースコーナー）。履歴＝ソリッドバッジ/ドット単体/1行目先頭すべて却下。
  - **エンゲージグリフ＝アウトラインのテキスト記号**（♡ ⇄ 🗨︎ 🔖︎ 👁︎・U+FE0E 強制）。カラー絵文字もモノトーンSVGも却下。ゼロ非表示・単位語なし（グリフが単位）。全面で同一セット。
  - フル日時はツールチップとインスペクタへ（日付2行をカードに並べない）。

### フィルタの分類（IA）

「何を問うか」の軸で分け**名前の衝突を避ける**:
- **複数画像＝メディアフライアウトの中**（`__multi`・画像/動画/GIFと同居）。旧・独立チェックボックス廃止。グループ単位判定なので `multiOnly` をトグル（per-record の mediaType とは別経路）。
- **「メディア/ポスト」(userKind) は廃止**（区別はタグで足りる）。レンダラ側 userKind 参照は撤去済み。既存サイドカーの userKind フィールドは無害（main.js の許可リストは別タスクで撤去予定）。
- **「種別」→「取得元」に再設計**: 本質は **url の有無**＝拡張取得（url有り）か手動取り込み画像（url無し）か。値キーは 'post'/'image'（互換）、述語 `!!p.url`、ラベル「SNS投稿/取り込み画像」。
- **「メディア」→「メディアタイプ」に改名**（画像/動画/GIF/複数画像）。
- **フライアウトに見出し行を出さない**（押した行は分かっている・繰り返すと邪魔）。`qfHeading`/`qf-back` 撤去。

### 状態の見せ方

- 件数バッジは tinted（カテゴリ行右端）。
- アクティブ条件はクエリビルダーバー（常時表示）に集約。フラットに並び、かつ/または はクリックトグル・ドラッグで括弧・ホバー✕で削除・右クリックで否定/削除（改訂③）。ⓘで説明ポップ。
- **空状態には必ず「次にやること」の導線**（CTA or ショートカットヒント）。
- フィルタから外れるミューテーションは即座に消さない（sticky-visible＋トースト・次の更新まで残す）。

## リファレンス

### 却下済みデザイン（再提案しない）

- 「よく使うタグ」frecency 自動表示 → 認知負荷。
- 「＋フィルタ」集約ポップオーバー → クリック過多。**カテゴリ行＋フライアウト**で代替。
- アクティブ状態の塗り（ソリッド／accent tint 地）→ チープ／色の使いすぎで没個性 → **モノトーンのコントラスト**で代替。
- タグチップの3状態サイクル → 分かりづらい。**トグル**で代替（ドラッグでのゾーン移動も 2026-06-18 撤回、改訂③でバー上の連結語クリック＋ドラッグへ）。
- サイドバー行を塗り＋枠線の箱で並べる → ワイヤーフレーム/Word図形感。**枠なし行＋ホバー塗り＋行頭アイコン**で代替。
- アクセント色のホバーハイライト → にじむ＋状態の意味が薄まる。**ニュートラル白/グレー**で代替。
- クエリビルダーの「かつ箱/またはゾーン＋ドラッグ移動＋接続プルダウン」＋「型ごとファセット＋演算子 select」→ 演算子分散で直感とズレ → **改訂③（バー上ドラッグ式インライン・ビルダー）で撤回**。自動型まとめ・per-type select・入れ子パネルは廃止。

### 共通パーツ（実装の単一真実源）

新規UIはこの対応表のパーツを使う（独自に再実装しない・Phase 4 で確立）。

| 役割 | 使うもの | 形・状態 |
|---|---|---|
| トースト | `corpusUI.notify()`（`#ivToast`・1.4s） | ガラス＝浮遊面。独自トースト禁止 |
| アイコンボタン | `.icon-btn`（26×26固定）／軽い＝`.icon-btn--ghost` | **サイズは `.icon-btn` に一元化**。`width/height` 個別上書き禁止 |
| ガラス材質 | 背後で選ぶ3層 A=`.glass-lens` / B=`.glass-frost` / C=`.glass-relief`（既存 frosted `--glass-*` は継続） | 末尾ユーティリティ層 |
| HTMLエスケープ | `corpusUI.escapeHtml()` | 重複実装を作らない |
| 操作ボタン | `.btn-outline`（通常）／`.btn-danger`（破壊） | 角丸四角＝操作。ピルで操作ボタンを作らない |
| フィルタ値チップ | `.sb-chip`（サイドバー）／`.sb-active-chip`＋`qc-*`（バー） | 丸ピル＝値。active は tint |
| アクティブ状態 | **モノトーンのコントラスト**（`--accent-subtle` 系＝淡グレー面＋`--text-strong`） | アクティブはグレー、色は機能的一点だけ。ソリッド塗り禁止 |
| 件数バッジ | muted 真円 `.sb-row-badge`（16px）。フィルタもWSも同じパーツ | 量＝muted。1桁=真円／2桁以上=自動ピル |
| コントラスト | サイドバーのチップ/バッジは**可視縁（`--border`）を必ず持つ** | 地の tint だけだと muted 背景で消える＝コントラスト問題の根治 |
| hover-scale | `scale(1.02)`＋影リフトは**コンテンツ項目とサムネ/浮くボタン**のみ。ナビ行/トグル/チップには付けない | 「持ち上がる」感。何でも拡大は toy っぽく却下。reduced-motion でオフ |
| メニュー／ポップ | `.fold-menu`／`.qf-popover` | フライアウト＝ガラス |

- `ui.js` は `index.html` で `i18n.js → ui.js → folders.js → viewer.js` の順に読込（`corpusUI` を後続が参照）。