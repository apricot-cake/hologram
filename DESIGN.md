# Corpus デザイン言語

ビューア（`app/renderer/`）の UI を変更するときの規範。トークンの実体は
[`app/renderer/design-tokens.css`](app/renderer/design-tokens.css)（コンポーネントは
semantic 変数のみ参照、primitive 直書き禁止）。

## 原則

**原則: 新しい形・色・動きを増やす前に、既存の語彙で表現できないかを必ず先に考える。**

**原則: 新しいUIパターンは必ず実プロダクトの前例（Apple HIG / Fluent / macOS / iOS / Finder / Notion / Linear 等）に錨を下ろす。** 独自の感性で発明しない（ユーザー要望: 「デザイン面は実プロダクトを積極的に参照」）。提案時は「どのプロダクトのどのパターンか」を一言添え、採用したらこのファイルに前例ごと記録する。

## 基礎

### 立ち位置（エトス・2026-06-20）

**Corpus は「企業的無機質さ」への対抗に立つ＝個人が作った・あなたのもの・とっつきやすい道具。** 出発点は「これ以上プラットフォームに自由を奪われたくない」（だからオフライン所有・投稿を丸ごと手元に）。「難解なソフトはもう使いたくない」という素朴で蓄積された欲求にも応える。スピリットは方向性の一部として持つ（厳密な憲章ではない）。

- **姿勢の前例＝Misskey**（OSS・連合・自前ホスト＝囲い込まれない／人懐く非企業的）。借りるのは**“姿勢”であって“様式”ではない**＝無機質の倒し方は Misskey の表現過剰でなく、Corpus の**静かなクラフトと抑制**（モノクロ＋ガラス／枠だけの透明箱を作らない／アクセント1点／モーション最小）。「Misskey の魂で、Corpus の所作」。
- **表現力は手放さず、層にする（低い床／高い天井）**: パワー（クエリビルダー等）は全部残すが、**最初に触る面の単純さを死守**し、深さは“消す”のでなく“潜れる所に置く”。フレンドリーさは*既定の面*の単純さで買い、コントロールは消さず潜らせる。前例＝Notion／Things／Misskey（ただ投稿＝床／MFM・絵文字＝天井）。
- **自由＝コントロールが“在る”×“押し付けない”**: プラットフォームは①コントロールを隠す ②ノイズで溺れさせる、で自由を奪う。ゆえに強力な機能を既定で畳むのは*不自由化でなく自由化*。クエリビルダーの表現力（＝マニュアル操作への誠実さ）は消さない。
- **運用の問い**: 機能ごとに「これは*床*（初手で出会う）か*天井*（潜って届く）か」を毎回問い、既定の面に出すのは床だけ。基準は「強力か」でなく「隠してもなお背負う概念的重さに見合うか」。「声と語り口」と段階的開示はこのエトスの実装。

### ブランド／ロゴ（2026-06-14 確定）

- **ロゴ＝波括弧 `{ ♡ }`**: 角丸の四角ハート（線画・塗りなし）を波括弧で囲む。波括弧＝**JSON サイドカー（`<id>.json`）の `{ }`＝投稿＋メタデータが構造化された塊**＝「投稿を丸ごと」の実体。ハート＝エンゲージメント（収集の動機）。`corpus`＝言葉／投稿の集成。**意味・実装・字体が三位一体**。波括弧と `#`（markdown 見出しに化ける）・角括弧（アイコンで括弧と認識されない）は検討の末いずれも不採用。
- **ワードマーク＝小文字 `corpus`（IBM Plex Mono Medium）**。バナー SVG では**アウトライン化（パス）**してフォント非依存にする（GitHub では webフォントが効かないため。`assets/banner-*.svg`）。
- **色**: 波括弧 ink `#23222c`/`#e9e7f3`、ハート＝**水色（sky）`#28a8db`/`#8ad3ec`**（アクセント1点。2026-06-18 にインディゴ `#6f68c8`/`#a59ee8` から変更＝アクセントを水色に統一したのに合わせた）。線幅 2.8、四角ハートは角丸（`stroke-linejoin:round`）。
- **アセット**: `assets/icon-{light,dark}.svg`（アプリ／ファビコン用・図形のみ）、`assets/banner-{light,dark}.svg`（アイコン＋ワードマーク＋タグライン）、`assets/logo-builder.html`（**後から調整して SVG を書き出せる**ツール・確定値で初期化）。
- **表記＝二段構え**（普通名詞 `corpus` の固有名詞性を守るため）:
  - ロゴ／GitHub リポ名／CLI・技術文脈 → 小文字 `corpus`
  - OS 表示名／`productName`／タイトルバー／README 見出し・散文 → `Corpus`
  - 前例: amazon・spotify（ロゴ小文字／表記キャピタライズ）。全小文字統一は普通名詞ゆえ埋もれるので不採用。
- **コピー**: タグライン「投稿を丸ごと。あなたの SNS ライブラリに。」／サブコピー「SNS投稿を、画像と情報ごとキャプチャ。自分のタグや、日付・エンゲージメント・投稿者などの条件で整理・検索できる、手元のアーカイブ。」**訴求の軸は「投稿」**（イラスト／画像は投稿の部分集合＝投稿でくくれば包含・逆は不可）。画像ライブラリの体験は UI（カード／タイル一覧）とサブコピーが担い、ロゴ・名前は「集成」を掲げる役割分担。

### 用語

- **投稿した人＝「投稿者」に統一（2026-06-15 決定）**。イラスト以外（写真・資料等）も保存するので「作者」は不正確＝不採用。アカウント/ユーザーも候補だったが「投稿者」に決定。**@ハンドルは「ユーザー名」**として別概念で残す（投稿者＝表示名／ユーザー名＝@handle）。全UI文言・i18n はこれに揃える（英語は Poster / Username）。「ユーザー」を人の意味で使わない。
- **2つの軸の呼称（2026-06-22 確定）＝〈何を見るか〉「ビュー」×〈どう並べるか〉「レイアウト」**。**ビュー**＝投稿／投稿者／コレクション（各「投稿者ビュー」等）。**レイアウト**＝カード／タイル／リスト。"閲覧／表示" は両方とも“見る系”で語感がかぶるため廃し、別フィールドの語（行き先＝ビュー／形＝レイアウト）に分けた。
  - **UI には抽象的な軸名（閲覧／表示／ビュー／レイアウト）を書かない＝具体物だけ**：ビュー切替トグルは行き先名（ライブラリ／投稿者）を直に出す（見出しなし）、レイアウト切替はアイコンのみ（旧「表示」見出しは撤去）。「ビュー」「レイアウト」は会話・docs 専用の語彙。
  - 投稿ビューの UI ラベルは「ライブラリ」（名詞「投稿（1件）」との混同回避・EN: Library / Posters）＝会話では「投稿ビュー」と呼ぶ。識別子（`browseMode`/`browsePosts`/`data-mode="posts"`/`.view-toggle`）は原語のまま。
- **ファセット件数（2026-06-26 確定）＝フィルタフライアウトの各値に「現在のクエリで絞った場合の件数」を出す機能の呼称**。標準語（faceted navigation の facet counts）に揃える＝将来コマンドパレット/全文検索などファセット文脈が増えても語を共有できる。件数降順・0件は末尾に沈め淡色。会話/docs では「ファセット件数」、UI部品としての呼称は「件数バッジ」（実装＝`facetTagCounts`）。

### 声と語り口（2026-06-20 確定）

**原則: ラベルとマイクロコピーは「何をするか／何が起きるか」を日常語で言う。** 抽象的なモード名（「通常」）やジャーゴンで済ませない。読み手が技術を知らなくても、語そのものが動作を語ること。

- **温度＝平易で温かい（friendly-plain）**。ただしスラング・ノリ・あざとさは避ける（流行り言葉は寿命が短くすぐ古びる）。砕けすぎず、お役所っぽくもない中間。
- **Corpus はこの声を持てる立場**: 個人OSS・単一ユーザー＝法人調達や多ブランド統一の制約が無い。むしろ「顔のない業務ツール」を避けられること自体が差別化になる。前例＝Mailchimp（人懐い文言）／Things（温かいマイクロコピー）／Linear（言い切る短文）／37signals（平たい物言い）。声はUXの資産。
- **破壊的操作には必ず安心を括弧で添える**（既存の慣行を明文化＝守る）: 「すべて削除しますか？（中の画像自体は消えません）」式。何が消えて何が残るかを、聞く前に言う。i18n に既にこの声が滲んでいる（`wsEmptyConfirm`/`foldDeleteConfirm`/`posterFolderDeleteConfirm` 等）＝それを規範として固定する。
- **砕けた語は正確さを1行で担保**: 日常語ラベルには短い補足（ヒント／ホバー title）を添える。例＝検索モード「おおまか」＋「タイプミスや言い回しの違いも拾う」。
- **1概念1語（割らない）**: 同じものを2つの語で呼ばない。温かい側に寄せて統一する（不統一の例＝詳細パネルの「エンゲージメント」とカードの「反応」）。
- **ドメイン語・識別子は原語のまま**（CLAUDE.md「技術用語・識別子は原語」と整合）: プラットフォーム名／SNS語（リポスト等）は無理に訳さない。日常語に直すのは「動作・状態・操作」を語る一般語だけ。
- **可逆だから踏み込める**: 文言は i18n 文字列＝いつでも戻せる。方針として一度寄せ、肌に合わなければ温度を下げる。
- **第一適用＝検索モード**: 「通常／あいまい」→**「ぴったり／おおまか」**（EN: Exact / Loose）。「通常」は中身を語らず、「あいまい」は“結果がいいかげん”と誤読されうるため。**UI＝両方を常に見せる macOS/iOS スコープバー式のセグメント切替**（`.seg-control`・凹トラック＋浮きつまみ `--seg-track`/`--seg-thumb`）＋選んだ方の動作を一行で補足（`.seg-hint`）。旧・検索欄右端の単独チップ（今のモードしか出ず「切替」と気づけない＝発見性が低い、の指摘）を置換。メイン検索とフィルタ・フライアウト（`.seg-control--qf`＝コンパクト版・ヒントはツールチップ）で共有。検索欄には虫眼鏡アイコンを添える（macOS の検索フィールド解剖）。

## ビジュアル言語

### 形の言語（シェイプ＝意味）

| 形 | 意味 | 例 |
|---|---|---|
| 丸ピル（`--radius-pill`） | **値**（フィルタ値・トグル対象） | タグ/投稿者/プラットフォームのチップ、クエリビルダーのピル |
| 角丸四角（6–8px） | **場所・操作**（押すと開く/起こる） | サイドバーのカテゴリ行、タググループ、CTA、各種ボタン |
| ○リング（塗りなし） | **選択** | カード左上の選択リング（ホバーで出現） |

この対応は崩さない。「値なのに四角」「ナビなのにピル」を作らない。

- **サイドバーのカテゴリ行は「枠なしテキスト行」**: 静止時は背景透明・枠なしでテキストとアイコンが面に直接置かれ、ホバー時のみ `--hover` の柔らかい塗り（Finder/Notion式）。**全行を塗り＋枠線の箱にするのは却下**（「Wordで図形挿入してテキスト打った」ワイヤーフレーム感＝ユーザー指摘）。枠線は入力欄（検索・select）にだけ許す。
- **カテゴリ行には行頭アイコン（14px・lucide系モノトーンSVG・`--text-muted`）**。同形のテキスト行が並ぶと無個性で生成AI感が出る。行ホバーでアイコンも `--text` に持ち上げ。
- **シェブロン（展開/飛び出しインジケータ）＝ くの字 SVG（細線・塗りなし）**。▸/▾ などの塗り三角は使わない。サイドバー行・サブ行・ソートドロップダウン等すべての展開インジケータを `.sb-row-arrow, .sb-subrow-arrow, .cs-btn .cs-arrow` の1ブロックで管理（`--text-muted`・11×11px）。折りたたみ開閉は `transform: rotate(-90deg)` で方向を切替（下向き↓＝展開済み、横向き→＝折りたたみ済み/flyout示唆）。新たな展開インジケータを追加する場合はこのブロックに追記し、文字コードや個別色指定で実装しない。
- **「枠だけの透明箱」を作らない＝形は塗りで作る**（枠線箱の量産がワイヤーフレーム感の正体）:
  - 値チップ＝**塗りピル・枠なし**（GitHub topics / Notion タグ）。**塗りは載る背景から明確に1段離す**（`--chip-bg`/`--chip-bg-hover`。`--surface-3` はサイドバー背景と5%差しかなく「塗りなのか汚れなのか分からないノイズ」になった＝ユーザー指摘）。ホバーはさらに1段上げ。アクティブ tint の縁だけが枠を持つ。
  - セグメンテッドコントロール＝**凹トラック＋浮きつまみ**（macOS式。`--seg-track`/`--seg-thumb`、つまみに `--shadow-xs`）。外枠・仕切り線なし。**レイアウト切替はアイコングリフ**（カード/タイル/リスト）＝**非アクティブはアウトライン（`fill:none;stroke`）でアイコンのみ・アクティブは塗り（`fill:currentColor`）＋テキストラベルを表示**（icon+label）＝iOS/macOSのタブバー語彙。**ツールチップは付けない**（アクティブのラベルが手がかり・ユーザー指定）。**非アクティブのホバーはグリフの明度だけ上げる（背景は変えない）**＝動く塗りはつまみだけ。
  - アイコンボタン（←→履歴等）＝**ゴースト**（素のグリフ、ホバーで `--hover` 塗り）。
  - 入力欄（検索/select/日付/数値）＝**凹んだ塗り（`--field-bg`）＋見える縁（`--border-strong`）**＝macOSの検索フィールド解剖。フィールドは「打ち込める窪み」として読めること。`--border-subtle` の極細縁は背景に埋もれて却下（ユーザー指摘）。「枠だけの透明箱」が許されるのは focus リングとアクティブ tint のみ。
- **プルダウン（select）はカスタムグラスドロップダウン**: native `<select>` のポップアップは OS 描画でガラスにできないため、select を `display:none`（値の保持元・既存の change ハンドラは生きる）にして、フィールド体裁のトリガー（`.cs-btn`・凹みフィールドと同じ見た目）＋ `.fold-menu` を流用したグラスのオプション一覧（`.cs-pop`）で置換する（`enhanceSelect()`）。開いた一覧がコンテンツの上に浮く＝フライアウトと同じガラス条件。値を外部から変えたら `refreshCustomSelects()` でトリガーのラベルを同期（restoreState/prefs/検索モード同期の3点）。対象はサイドバーの並び順・検索方式。設定モーダル内の select は対象外（モーダルは元々ガラスでない）。

### 色

- ベースはモノトーン（cool slate）＋**水色（sky）のアクセント**（`--sky-*` プリミティブ。2026-06-18 にインディゴから変更＝ユーザー決定）。アクセントは**2ステップ**で持つ: `--accent`（濃＝白文字/✓が乗る塗り: primaryボタン・選択チェック、および色文字: リンク・focus。`--sky-600`／ダーク`--sky-500`）と `--accent-soft`（淡＝上に何も乗らない輪郭: 選択リング・ロゴの線。`--sky-300`系）。前例: Linear / Stripe。**注意**: 水色は明るいので塗り（`--accent`）に乗る白文字/✓のコントラストは旧インディゴ（~8:1）より低い（~3:1）＝視認弱ければ塗りだけ一段濃くする（色相は維持）。
- **モノトーン基調＝アクセントは「機能的に意味のある一点」にだけ使う**（選択リング・選択チェック・primary・focus・リンク）。フィルタ値/タグ/ワークスペース/クエリビルダーの**アクティブ表現はモノトーンのコントラスト**にする（`--accent-subtle`系をグレーへ再定義済み＝淡グレー面＋`--text-strong`文字）。前例: Linear / Vercel / Notion / Things（ナビ選択はグレー、色は注意を引く一点だけ）。**旧「アクティブ＝accent tint」は撤回**（色の使いすぎは没個性＝ユーザー判断 2026-06-14）。**ソリッド塗り（`--accent` ベタ）のアクティブも禁止**（チープ）＝ソリッドが許されるのは danger と画面で唯一の真の primary だけ。
- **カテゴリ色分け（`--tint-*`）はアクセントとは別レイヤーで残す**: プラットフォーム/タグ/取得元などの「種別ラベルの色」は分類色なのでモノトーン基調でも保つ（Linear/Notion もラベルには色を使う）。クエリビルダーの条件ピルがこれ。
- **ホバー＝ニュートラル（モノトーンの薄い塗り）、アクセント＝選択/アクティブの「状態」**。この使い分けを崩さない（iPadOSポインタ/Fluent/Material 共通の定石）。ホバーにアクセント色を使うと、半透明の色がコンテンツ上で「にじんで」見える＋アクセントの状態表示としての意味が薄まる（ユーザー指摘済み）。例外は意味を持つホバー色のみ（取り消し予告の赤）。
- 破壊操作は `--danger`。**赤の color-mix は srgb で行う**（oklch だとダーク背景と混ざって紫に転ぶ・指摘済み）。
- アイコンはモノトーン SVG（`currentColor`）。絵文字アイコン・固有色アイコンを増やさない。

### ガラス（フロスト／レンズ）

**基準は面の分類＝「浮遊面」か「接地面」か**（旧「一時面だけ」を一般化。2026-06-14 改訂）。根拠は Fluent（Acrylic=浮く面、Mica=接地面）と Apple HIG（マテリアル＝下をコンテンツが流れる面）。

- **浮遊面（画面の上に浮く面）はすべてガラス可**: ポップ／右クリックメニュー／フライアウト／日付・エンゲージポップ／モーダル／トースト／sticky アクティブバー／スライドオーバー時インスペクタ／ライトボックス暗幕／カードのホバーボタン／プルダウン／最上部に戻るボタン。**背後にコンテンツが流れていれば最も映えるが、流れていなくても（単色 Mica の上でも）ガラスは使ってよい**＝半透明素材の質感として可。
- **接地面（レイアウトに固定される面）は Mica 材質**: サイドバー・カード・インライン時インスペクタ。下が単色でぼかす対象が無く「濁ったグレー」になる＋常時GPU負荷のため、ガラス（=ぼかし）でなく Mica（①薄い縦グラデ＝上端 `color-mix` 6%明・下端4%暗 ②微細ノイズ＝SVG `feTurbulence` data-URI・alpha 4.5% ③上端1pxハイライト）で質感を出す。サイドバーが実装例。**ただし接地面でも「動き/選択の瞬間」だけガラス化は可**（Apple Liquid Glass＝ガラスは静止塗りでなく*インタラクションで現れるレンズ*。レイアウト切替トグル等は静止時 Mica・ホバー/選択モーションにだけガラス）。

**ガラス材質システム＝「背後に何があるか」で3層を選ぶ**（`index.html` 末尾にユーティリティ層として定義＝`.fold-menu` 等のコンポーネント規則に後勝ち。ホストは自分で `position` を指定＝`::before` スペキュラのアンカー）。共通＝明るいリム＋斜めスペキュラ(`::before`)。
- **A `.glass-lens`（背後＝リッチなコンテンツ／カードの上）**: 高透過(surface 14%)＋薄ブラー(8px)＝**読み抜ける**。透過率の下限なし。**テキスト密なホストは材質を厚く**（Apple も dense text は ultraThin を使わない＝`.kb-hint-pop.glass-lens` は 42%）。実装例＝ショートカット一覧ポップ。
- **B `.glass-frost`（背後＝低コントラスト/低情報／プルダウンがサイドバーの上）**: 中不透明(55%)＋強ブラー(18px)＝**ブラーで霜を自前で作る**ので busy な背景に依存しない。実装例＝カスタムプルダウン `.cs-pop`。
- **C `.glass-relief`（背後＝何もない／トグルが平らな Mica の上）**: **内部に焼き込んだ上明→下暗グラデ**（＝背景が無くても屈折感を出す）＋共有スペキュラ＋明るいリム。backdrop-filter はほぼ効かない前提。実装例＝レイアウト切替トグル `.view-toggle`。
  - **トグルのつまみ＝フラットだがリッチなリキッドガラス（2026-06-21 改定・旧「凸ガラスビーズ」は撤回）**: レイアウト切替のつまみ（`.view-toggle .vt-thumb`）は**フラット**（ドーム反射`::before`・中央クリア→リムの凸 radial・内側ブルーム・凸リフト影・スライド時の bulge `vtBulge` は載せない＝「膨らんだプラスチックのビーズに見える」というユーザー指摘 2026-06-21 を解消）。ただし**平らすぎてコントラストが消えた**ので、フラットなまま**リッチなリキッドガラス**に寄せる＝①背後トラックを実際に歪ませる屈折（`backdrop-filter: url(#glassRefract)`）②明るく彩度を上げた frost フィル（トラックとの**コントラスト確保**）③定義された外周ヘアライン＋**上端の光のライン**（`inset 0 1px 0` ＝ガラスが上縁で光を受ける表現・ドームではない）④柔らかい落ち影。light/dark とも同方針。前例＝Apple Liquid Glass（屈折・縁の光）／macOS セグメンテッド（平らに滑る）。凸/凹の錯覚は避け、リッチさは屈折と縁の光で出す。
- **本物の屈折＝SVG変位フィルタ `#glassRefract`（2026-06-16）**: **コンテンツの上に浮く面**（A/B＝フライアウト等）は `backdrop-filter: url(#glassRefract)` で背後を**実際に歪ませる**（`feTurbulence`→`feDisplacementMap`＋軽い `feGaussianBlur`）＝CSS の blur が「ぼかすだけ・位置はそのまま」なのに対し、これは背景が**波打って曲がる**本物の屈折。小領域・静的なら廉価PCでも軽い（重いのは大面積/アニメ/スクロールし続ける背後）。適用面＝`.fold-menu`（フライアウト/メニュー）／`.qf-popover`（日付・エンゲージ）／`.inspector`（浮遊時）／`#sbTop`。**除外**＝①設定モーダル（大面積＋暗幕の上＝屈折しても映る中身が無い＋重い→ blur 据え置き）②`.glass-lens`（読み抜け用＝背後を歪ませると読めなくなる）。**歪みは控えめ厳守**（強すぎ＝生成AI感／下のハード制約）。背後が平らな C には効かない（C は上の凸ビーズで対応）。フライアウト行の**ホバーは半透明**（`--sidebar-row-hover`）＝不透明塗りはガラスに浮くため。
- **旧 frosted トークン（`--glass-filter`/`--glass-bg`系）** はバー/トースト等で継続使用（`.qf-popover`/`#ivToast`）。新規はなるべく上の3クラス＋必要なら `#glassRefract` に寄せる。

**ハード制約（実験でも守る＝却下済み）**:
- ガラスを重ねない（glass-on-glass は濁る）。
- 色収差はエッジ局所で控えめに（強すぎは生成AI感で却下）。
- アクセント色のソリッドなブロブをサムネ上に置かない（にじむ。→ `.act-pill` はニュートラル白）。
- 透明の混合は **srgb**（赤と同じ理由）。`prefers-reduced-transparency: reduce` でソリッドに戻すエスケープハッチ必須。

**ガイドライン（推奨・実験で外してよい）**:
- 浮遊面は背後にコンテンツが流れる位置だと最も映える（必須ではない＝単色上でも可）。
- frosted の tint は小文字を重ねる面では 70% 前後を目安に（読みやすさ優先。**旧「70%未満禁止」はハード制約から格下げ**＝lens のように読み抜ける設計なら下回ってよい）。
- backdrop-filter は数を絞る（GPU負荷）。例: カードのホバーボタンは**ホバー中のカードに限定**して有効化（全カード常時レイヤ化は却下）。

**リキッドホバー（`.act-pill`）**: 🔖↔ℹ 間を**ニュートラル白（rgba 255/0.25）**の光の膜が滑って追従（iPadOSポインタ式）。`:has()` 駆動のCSSのみ・JSなし。**ピルはボタンの上層（z2）に重ねる**（下層だとダークガラス越しでスライドが知覚できない＝ユーザー報告済み）。状態色（赤=外す/緑=追加済み）は半透明膜越しでも判読可。`prefers-reduced-motion` ではフェードのみ。

### モーション

- 150–260ms / ease-out（`--ease-out`・`--dur-*`）。出現は fade ＋ 4–6px rise（`corpusFadeIn` / `corpusPopIn`）。
- 入場アニメは「新規構築」時のみ＝初回ロード・フィルタ/検索/ソート/レイアウト切替。**ミューテーション（ワークスペース/フォルダの追加解除・削除・グループ変更・タグ保存）とバックグラウンド更新（fs-watch・一括削除後の再読込）では再生しない**（keepLimit 再描画を使う。「画面がリフレッシュした」ように見える＝ユーザー指摘済み）。ページ送り（load-more）も再生しない。
- `prefers-reduced-motion: reduce` で必ず無効化する。

## レイアウトと余白

### レイアウト（全体構造・2026-06-16）

- **クエリビルダーは全幅の上部バー**（投稿ビュー `#postActiveBar`／投稿者ビュー `#posterActiveBar`＝tabBar 直下・モードで出し分け・常時表示）。左端まで伸びてサイドバーの上を貫き、最上段を全幅で占有する。バー高さは JS が `--activebar-h` に実測反映。**両ビューは同一のビルダー実装（`createQueryBuilder` factory・投稿=postQB／投稿者=posterQB）を共有**。前例＝Claude デスクトップアプリ（上部に横断バー）。
- **サイドバーは「ウィンドウ」として浮かせる**: クエリビルダーの下から始まり、角丸 14px・四辺 `--sidebar-float`(10px) の余白・定義された枠（`--glass-border`）＋ `--shadow-md` で独立した浮遊カードにする。sticky top はクエリバー＋余白の分だけオフセット（スクロールで天井に貼り付く）。前例＝Claude デスクトップアプリのサイドバー。
- 旧・サイドバー↔本文の凹フィレットは廃止（全幅化で接合コーナーが消えたため）。
- **背景グリッド（方眼・2026-06-18）**: コンテンツ背後（`#appBody`）に薄い方眼を常時敷く＝`color-mix(var(--text) 8%)` の 1px 線・26px セル・テーマ自動追従。控えめなテクスチャでフラットな地を引き締める。前例＝方眼/ドット背景のキャンバス系UI（Figma/Miro 等）。**ガラスサイドバーで背景を屈折させて見せる案は試したが不採用**（`#glassRefract` の blur で細線が消える・ガラス感とグリッドの両立が難しい＝2026-06-18 のデザイン実験で判断）。

### 余白・寸法のリズム

- **サイドバーの横方向の軸は `--sb-gutter`（1変数）で一元管理**。`.sb-scroll` がこの inset を持ち（`padding-left: calc(6px + gutter)` / `padding-right: calc(8px + gutter)`＝6pxは選択リングの負マージン逃がし・8pxはスクロールバー間隔）、**各要素は左右パディングを持たず full-width**。これで全要素の箱の左右が構造的に揃う＝個別の magic number（旧 12/13/14px のバラ付き）を撤去。「バーが横に飛び出す／揃わない」はこれで解決（ユーザー指摘）。軸を動かすときは gutter 1か所だけ変える。
  - **2つの軸**: セクション見出し（`.sb-title`）は padding 0 で**ガター（箱の左端）**に置く＝一段外。フィルタ行アイコン／バー文字／チップなどの**コンテンツは内側 1 content-pad（≈12px）**＝一段内。見出しが外・中身が内、で階層が読める（Notion/Finder）。
  - サブセクション（`.sb-subtitle`＝ピン留め/グループ）は**タグの子**: セクション見出しより小さく薄く（10px `--text-subtle`）＋ content-pad 分インデント。同インデント・同サイズで親と区別不能だった件の解消（ユーザー指摘）。
- 大セクション（フィルタ/タグ/ワークスペース/フォルダ）は**余白で分ける（26px）＝区切り線は使わない**（ユーザー却下。線より余白で構造を出す）。
- **フィーチャー起動ボタン（連続タグ付け等）＝枠なし塗り（`--chip-bg`）＋アクセント色アイコン**。チップ言語に合わせ、アイコンの色だけで「機能がある」を示す。**枠付きtint箱は却下**（再デザイン前の浮いた見た目・ユーザー指摘）。
- サイドバーの横長コントロールは**上下スリム**（カテゴリ行・レイアウト切替=5px、select=5px、検索=6px の縦パディング）。ダイアログ既定（8px）をそのまま持ち込まない。
- グリッド側コンテンツ pad: 24px 32px。

## インタラクションと情報設計

### 操作語彙

- **クリック＝トグル**（追加/解除）。3状態サイクルは廃止済み — 復活させない。
- **クエリビルダー＝バー上のドラッグ式インライン・ビルダー**（改訂③・2026-06-19 実装）。条件は種類に関係なくフラットに並び**トップ階層＝かつ**。**演算子（かつ/または）は連結語のクリックでグループ単位トグル**、**ピル→ピルのドラッグで括弧グループ化**（混ぜたいときだけグループ化＝括弧の存在理由・ドラッグで作った群は親の逆）、**ホバー ≠ で否定**、**「グループで囲う」で全体を一括ネスト**。`queryTree` が単一真実源（ブール完全）。旧「種類ごとのファセット＋per-type 演算子 select／入れ子パネル」は撤回。前例: ドラッグで括弧を組む式エディタ。詳細は docs/design-query-builder.md「改訂③」。
- **ホバー＝最頻アクションだけ（カードは🔖・ℹ・🏷の3個まで）**。取り消し系アクションはホバー時に赤（控えめな赤）。
- **右クリック＝そのカードの全アクションの目次**（開く/タグ編集/フォルダ/ワークスペース/詳細/投稿者/削除）。低頻度・破壊的・「選ぶ」系の操作はホバーに並べず右クリックメニューへ。ラベル＋アイコン付き行（fold-menu 様式）、破壊行は赤（`fm-danger`・srgb 混合）。ホバーアイコンを4個以上に増やす変更は飽和の再発＝却下。
- **フライアウト**＝サイドバー行の右に開く（カテゴリ→値）。値が8件超なら絞り込み入力を付ける（**ただしプラットフォームは短い固定リストなので絞り込み入力なし**）。**日付・エンゲージのパラメータ入力ポップも行の右に開く**（真下出しは下の行を覆って切替しづらい・ユーザー指摘）＝全フライアウト/ポップを右出しで統一。
- **モーダル**＝設定・確認のみ。中央寄せ＋backdrop。全データ削除などはキーワード入力ゲート必須。
- **インスペクタ**＝ℹ で右カラムに常設表示（非モーダル）。広い幅はインライン、狭い幅（<1280px）は**同じパネル**がスライドオーバーに（消えない・レイヤリングだけ変わる）。表示中は対象カードに tint リング。インライン時はカードクリックで内容差し替え（ダブルクリックでギャラリー）。**スライドオーバー時はパネル外（グリッド/カード）のクリックで閉じる**（クリックは消費＝同じ押下でカードは反応しない。ℹ だけは差し替えとして生きる）。Esc/× でも閉じる（上に乗るオーバーレイが先に Esc を消費）。**投稿インスペクタの投稿者名/アバターはその投稿者ビューへのリンク**（双方向ナビ＝投稿→投稿者。投稿者→投稿一覧の逆向き。投稿者ビューに存在する SNS 投稿のみ・右クリック「この投稿者を見る」も同義）。
- **ピン＝ユーザー制御の常駐化**。頻度ベースの自動表示 UI は採用しない（認知負荷・却下済み）。
- **クリップ vs コレクション**＝「ライブラリ上の一時フラグ」と「永続の棚」。**クリップ**はカードのホバーボタン（📎・lucide paperclip）ワンクリックで付ける一時の印（単一・迷いゼロ）。コレクション（フォルダ）は複数・名前付き・右クリック→ピッカーで追加先を選ぶ（明示）。**デフォルトフォルダは廃止**（ワンクリック自動追加は「1つしか入れられない」誤解と固さを生んだ）。**クリップ済み＝ホバーボタンがアクセント色に反転**（塗りつぶしトグルの「on」表示＝GitHubスター済み/iOSいいね済みの語彙。旧・ベタ塗り却下＋小さなアクセントドット `.clip-btn.in::after` は、この色反転に置換 2026-06-27）。**ホバー赤（外す予告）は廃止**（クリップは気軽な頻繁トグルで赤は大げさ・ユーザー 2026-06-27）。サイドバーの「クリップ」行はフィルタ行群の先頭（プラットフォームの上）に他フィルタと同列・同間隔で並び、クリックで「クリップ付きだけ」に絞る。一括解除は**「空にする」＋確認ダイアログ**（「クリア」表記はフィルタ解除と誤読されるため却下。投稿自体は消えないことを必ず明記）。クリップは `collections.json` トップレベルの独立フラグ集合（captureId の Set・コレクション配列とは別レイヤー・クリアまで永続）。**旧「アクティブ(★)＝🔖ワンクリック投入先」は廃止**＝コレクションへの投入は右クリック「コレクションに追加」（ピッカー）で行う。
- **コレクションビュー（第3ビュー）＝コレクションの一覧面**。カード＝名前＋件数＋サムネ（件数別モザイクで正方を詰める＝固定2×2の歯抜けを廃し1=全面/2=左右/3=大+2段/4=2×2・空はレイヤーグリフ）＋名前の前に動的の⚡（静的は無印・サムネには重ねない）。**クリック＝開く**、**右クリック＝目次**、**戻る＝常設のビュー切替トグル「コレクション」**（追加フィルタ後でも1クリックで一覧へ＝戻るボタンに依存しない）。**静的（手集め）と動的（保存検索）が同じグリッドに共存**＝静的はフォルダフィルタを流用、動的は保存した条件（クエリツリー＋検索語）を**今のライブラリに当てて件数/サムネを実時間算出**（＝開けば最新）＋名前下に条件の小チップ。**入口＝投稿ビューのクエリ帯「検索を保存」ボタン**（絞り込みがある時だけ出る・枠なし塗り＋アクセント色で「機能がある」を示す）で名前を付けて動的化（「残せば資料・残さなければ探索」）。**クリックで開く＝保存条件を復元**（その場でいじって再保存可）。**旧・アクティブ(★)は廃止**（コレクションへの投入は右クリック「コレクションに追加」のピッカーで）・動的コレクションの右クリックは**「条件を今の絞り込みで更新」**。
- **タグ付け＝2軸の直接操作（モーダル/ウィザード廃止・2026-06-13設計／2026-06-15実装）**。グリッド上部に出る**スリムなタグ付けツールバー**（軸トグル＋装填中タグ＋完了だけ。クエリビルダー帯と2段で共存）で2軸をトグル切替。①**カード編集**＝カードをクリック→その1枚の編集モーダル（現在のタグ＋**グループ別の全タグをクリックで付け外し**＝recognition over recall＋入力で新規作成。1画像→複数タグ）。②**スタンプ**＝タグを1つ"装填"→カードを連続クリックで付与/解除トグル（1タグ→複数画像・**Lightroom Painter/スプレー式が出典**）。既存タグには足すだけ（置換しない）。
  - **スタンプのタグパレットは左サイドバーへ置く（横長ツールバーは幅不足でタグ付けに不向き、の指摘・2026-06-15）**。スタンプ軸の間は左サイドバー（フィルタ）を**縦・グループ別・検索付きのパレット**に切り替える（上＝検索、中＝グループ別タグ、下＝新規タグ/グループ作成を常設）。サイドバーは元々この縦・グループ・検索の構造を持つので転用が自然。絞り込みは「タグ付けに入る前」に行い、入った後も上部の絞り込み帯で現在の条件は確認/解除できる。カード編集軸ではサイドバーはフィルタへ戻る（編集はモーダルで完結）。
  - 付いたカードは**選択リングと同じ枠オーバーレイ（アクセント淡）＋小さなアクセント丸＋白チェック**で示す（inset box-shadow＋✓グリフは縁が不均一で却下）。トグルは**モノトーンのコントラスト**の segmented（accent tint 地は使わない＝撤回済み規約）、装填中タグは**アクセント淡のエッジのみ**（ソリッド塗り禁止）。**祝祭エフェクトは撤去**（2026-06-15・うるさい/不要の判断。コードは git 履歴に）。
  - 旧「連続タグ付けウィザード」は**退役**。UI文言で「ウィザード」等の開発者語彙は使わない。レコメンド/自動候補は不採用（決定的・ユーザー主導）。タグ検索はパレット/編集モーダルの検索欄でグループ横断に効く。
- ショートカット: `Alt+S`（拡張・キャプチャ）/ `Ctrl+A`（全選択）/ `/`・`Ctrl+K`（検索フォーカス）。
  グローバルショートカットは「入力中・オーバーレイ表示中は奪わない」ガードを必ず入れる（Ctrl+A ハンドラが雛形）。

### カードの情報階層（Ivory/Tweetbot のセル解剖に準拠）

- **1行目**: 投稿者名（600）＋ @handle（subtle・省略）のみ。
- **フラグ**（リプライ/引用/スレッド/メディア種）: 淡色テキストのみ。枠・塗りのチップにしない。
- **本文**: 3行クランプ（リストは2行）。
- **最終行（`.post-foot`）**: 左=非ゼロのエンゲージのみ、右=**出典コーナー（`.foot-r`）**＝PFラベル＋コンパクト日付（同年=M/D）の1クラスタ。
  - **PFラベル（`.pf-tag`）＝ブランドドット7px＋mutedキャップス10px**、置き場所は**フッター右**（Reederのソースコーナー）。履歴: ソリッドバッジ却下（最うるさいのに情報量最少）→ ドット単体却下（判別不能）→ 1行目先頭も却下（投稿者名/ファイル名と密着して紐づかない・subtleでは弱すぎ）。
  - **エンゲージのグリフ＝アウトラインのテキスト記号（♡ ⇄ 🗨︎ 🔖︎ 👁︎・テキスト表示強制 U+FE0E）**。カラー絵文字もモノトーンSVGも却下（ユーザー選定）。**ゼロ非表示・「RT」等の単位語なし**＝グリフが単位。カード/タイルオーバーレイ/インスペクタで同一セット。
  - 投稿/キャプチャのフル日時はツールチップとインスペクタへ — 日付2行をカードに並べない（疎密が崩れる・ユーザー指摘）。

### フィルタの分類（IA）

フィルタは「何を問うか」の軸で分け、**名前の衝突を避ける**（種別/メディア/メディア・ポストが「画像/メディア/ポスト」を共有して混乱、とユーザー指摘）。
- **複数画像**は「添付の性質」なので **メディアフライアウトの中**（`__multi`・画像/動画/GIFと同居）。旧・独立チェックボックスは廃止。ただしグループ単位の判定（`g.files.length>1`）なのでフィルタ配列ではなく `multiOnly` をトグルする（per-record の mediaType フィルタとは別経路）。メディア行バッジは multiOnly も加算。
- **「メディア/ポスト」(userKind) は廃止**（フィルタ行＋連続タグ付けのチョイサーとも）。「単なる投稿」の区別はタグで足りる＝専用システム不要（ユーザー判断）。レンダラ側の userKind 参照（述語・フィルタ値・ラベル・アイコン・i18n）は撤去済みで一切読まれない（2026-06-20 のデッドコード掃除）。既存サイドカーに残る userKind フィールドは無害（main.js の update-tags 許可リストだけ別タスクで撤去予定）。
- **「種別」→「取得元」に再設計**: キャプチャ/画像の切替は直感的でなかった。本質は **url の有無**＝拡張で取得（SNS投稿: キャプチャもドラッグも url 有り）か、手動取り込み画像（url 無し）か。キャプチャ/ドラッグの別は区別する価値がないので採らない。値キーは 'post'/'image' のまま（保存状態の互換）、述語を `!!p.url`、ラベルは「SNS投稿/取り込み画像」、行アイコンはリンク。
- **「メディア」→「メディアタイプ」に改名**（添付の種類: 画像/動画/GIF/複数画像 と明確化）。
- **フライアウトに見出し行を出さない**: ユーザーは押した行を分かっているので、フライアウト先頭で行名を繰り返すと「選べる行」に見えて邪魔（ユーザー指摘）。`qfHeading`/`qf-back` 撤去。

### 状態の見せ方

- 件数バッジは tinted（カテゴリ行の右端）。
- アクティブな条件はクエリビルダーバー（常時表示）に集約。**条件はフラットに並び（種類でまとめ
  ない）、メンバー間の「かつ/または」はクリックでトグル**。ドラッグで括弧グループ化・ホバー ✕ で
  削除・右クリックメニューで否定/削除（改訂③）。ⓘ で説明ポップを出す。
- **空状態には必ず「次にやること」の導線**（CTAボタン or ショートカットヒント）を置く。
- フィルタ条件から外れるミューテーションは即座に画面から消さない（sticky-visible ＋トースト、次の画面更新まで残す）。

## リファレンス

### 却下済みデザイン（再提案しない）

- 「よく使うタグ」frecency 自動表示 → 認知負荷。**ピン留め**で代替。
- 「＋フィルタ」集約ポップオーバー → クリック数過多。**カテゴリ行＋フライアウト**で代替。
- アクティブ状態の塗り → ソリッドはチープ／accent tint（`--accent-subtle` 地）も色の使いすぎで没個性 → **モノトーンのコントラスト**で代替（アクセントは機能的一点のみ・2026-06-14）。
- タグチップの3状態サイクル（or→and→off） → 分かりづらい。**トグル**で代替（ドラッグでのゾーン移動は 2026-06-18 に撤回。さらに 2026-06-19 改訂③で演算子はバー上の連結語クリック＋ドラッグ・グループ化へ）。
- サイドバー行を塗り＋枠線の箱で並べる → ワイヤーフレーム/Word図形感。**枠なし行＋ホバー塗り＋行頭アイコン**で代替。
- アクセント色のホバーハイライト（リキッドピル等） → コンテンツ上でにじんで見える＋「状態」の意味が薄まる。**ニュートラル白/グレー**で代替（ホバー=位置、アクセント=状態）。
- クエリビルダーの「かつ箱／またはゾーン＋ドラッグ移動＋接続プルダウン」 → 演算子が3か所に分散＋一番押せそうなピル間の演算子表記が非操作＝直感とズレ → いったん「種類ごとのファセット＋演算子 select」(2026-06-18) → **2026-06-19 改訂③で撤回**（現行＝バー上のドラッグ式インライン・ビルダー。設計詳細は上「操作語彙」＋ docs/design-query-builder.md「改訂③」）。自動型まとめ・per-type select・入れ子パネルは廃止。

### 共通パーツ（実装の単一真実源）

新規UIはこの対応表のパーツを使う（独自に再実装しない）。Phase 4 の統合で確立。

| 役割 | 使うもの | 形・状態 |
|---|---|---|
| 一時通知（トースト） | `ui.js` の `corpusUI.notify()`（ガラス `#ivToast`・1.4s） | ガラス＝浮遊面。独自トーストを作らない |
| アイコンボタン | `.icon-btn`（26×26 固定）／軽い見た目は `.icon-btn--ghost` 修飾子 | **サイズは `.icon-btn` に一元化**＝隣と上下端が構造的に揃う。`width/height` を個別上書きしない（揃わなくなる） |
| ガラス材質 | **背後で選ぶ3層**: A=`.glass-lens`(コンテンツ上) / B=`.glass-frost`(低情報背景・プルダウン) / C=`.glass-relief`(背景なし・トグル)。既存 frosted=`--glass-*` は継続 | 末尾ユーティリティ層。詳細は「ガラス（フロスト／レンズ）」節 |
| HTMLエスケープ | `ui.js` の `corpusUI.escapeHtml()`（`& < > " '` を全てエスケープ） | viewer/folders とも委譲。重複実装を作らない |
| 操作ボタン | `.btn-outline`（通常）／`.btn-danger`（破壊・赤枠赤文字） | 角丸四角＝操作。ピル（`.chip`）で操作ボタンを作らない |
| フィルタ値チップ | `.sb-chip`（サイドバー）／`.sb-active-chip`＋`qc-*`（アクティブバー） | 丸ピル＝値。active は tint |
| アクティブ状態 | **モノトーンのコントラスト**（`--accent-subtle`系＝淡グレー面＋`--text-strong` 文字。accent tint は撤回） | 色の使いすぎは没個性。アクティブはグレー、色は機能的一点だけ（Linear/Vercel/Notion/Things）。ソリッド塗りも禁止 |
| 件数バッジ | muted 真円 `.sb-row-badge`（`--surface-3` 地＋`--border`＋`--text-muted`・16px・flex 中央）。フィルタ件数もワークスペース件数も**同じパーツ** | 量＝muted（accent 塗りは浮く）。1桁=真円／2桁以上=自動でピル |
| コントラスト（チップ/バッジ） | サイドバーのチップ/バッジは**可視縁（`--border`）を必ず持つ** | 地の tint だけに頼ると muted 背景で消える＝**再発するコントラスト問題の根治**。縁はコントラスト非依存で背景に左右されない |
| hover-scale | `transform: scale(1.02)`（＋影リフト・`z-index`前面化）は**コンテンツ項目（投稿/投稿者カード＝カード・タイル・リスト行で統一）とサムネ／浮くボタン（丸ボタン）**。サイドバーのナビ行・トグル・チップには付けない | 「持ち上がる」感（Pinterest）。リスト行も行ごと拡大に統一（旧「行には付けない／サムネだけ枠内ズーム」は撤回・2026-06-22）。何でも拡大は toy っぽく却下。`prefers-reduced-motion` でオフ |
| メニュー／ポップ | `.fold-menu`／`.qf-popover`（ガラス） | フライアウト＝ガラス |

- `ui.js` は `index.html` で `i18n.js → ui.js → folders.js → viewer.js` の順に読み込む（`corpusUI` を後続が参照するため先に）。
