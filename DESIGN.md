# Corpus デザイン言語

ビューア（`app/renderer/`）の UI を変更するときの規範。トークンの実体は
[`app/renderer/design-tokens.css`](app/renderer/design-tokens.css)（コンポーネントは
semantic 変数のみ参照、primitive 直書き禁止）。

**原則: 新しい形・色・動きを増やす前に、既存の語彙で表現できないかを必ず先に考える。**

## 形の言語（シェイプ＝意味）

| 形 | 意味 | 例 |
|---|---|---|
| 丸ピル（`--radius-pill`） | **値**（フィルタ値・トグル対象） | タグ/作者/プラットフォームのチップ、クエリビルダーのピル |
| 角丸四角（6–8px） | **場所・操作**（押すと開く/起こる） | サイドバーのカテゴリ行、タググループ、CTA、各種ボタン |
| ○リング（塗りなし） | **選択** | カード左上の選択リング（ホバーで出現） |

この対応は崩さない。「値なのに四角」「ナビなのにピル」を作らない。

## 色

- ベースはモノトーン（cool slate）＋アクセント1色（blue/indigo）。
- 選択・アクティブ状態は **tint**: `--accent-subtle` 背景 + `--accent-subtle-fg` 文字 + `--accent-border` 枠。
  **ソリッド塗り（`--accent` ベタ）のアクティブ状態は禁止**（チープに見える・ユーザー却下済み）。
  ソリッドが許されるのは danger 実行ボタンと、画面に1つの真の primary アクションのみ。
- 破壊操作は `--danger`。**赤の color-mix は srgb で行う**（oklch だとダーク背景と混ざって紫に転ぶ・指摘済み）。
- カテゴリ色分けバッジ/チップは `--tint-*` を使う。
- アイコンはモノトーン SVG（`currentColor`）。絵文字アイコン・固有色アイコンを増やさない。

## ガラス（フロスト）

- **ガラスは「コンテンツの上に浮く一時面」だけ**: 右クリックメニュー・フライアウト・日付/エンゲージポップ・stickyアクティブバー・スライドオーバー時のインスペクタ・ライトボックス暗幕・トースト。
  根拠は Fluent（Acrylic=トランジェント専用、常設はMica）と Apple HIG（マテリアル=下をコンテンツが流れる面）。
- **常設面（サイドバー・カード・インライン時のインスペクタ）はガラスにしない**。下がフラットな `--bg` でぼかす対象が無く「濁ったグレー」になるだけ＋常時GPU負荷。
  ただしフラット一色の「のっぺり」も却下（ユーザー指摘）→ 常設面は **Mica風の材質**で質感を出す: ①ごく薄い縦グラデ（上端を `color-mix` で6%明るく・下端を4%暗く）②微細ノイズ（SVG `feTurbulence` data-URI・alpha 4.5%）③上端1pxハイライト。サイドバーが実装例。ガラス（=ぼかし）と材質（=粒子とグラデ）の使い分けが Fluent の Acrylic/Mica に対応する。
- トークン: `--glass-bg`(tint78%・メニュー用) / `--glass-bg-bar`(70%・バー用) / `--glass-bg-panel`(88%・テキスト密な面用) / `--glass-filter`(blur24px+sat140%) / `--glass-edge`(上端1pxハイライト、darkでは透過7%)。
  **tint は 70% 未満に下げない**（12px文字をサムネの上で読む legibility 下限。装飾サンプルの「薄いほど綺麗」をUIに持ち込まない）。
- 透明の混合は **srgb**（赤と同じ理由）。`prefers-reduced-transparency: reduce` でソリッドに戻すエスケープハッチ必須。
- ガラスを重ねない（glass-on-glass は濁る）。新たな面に足すときは「一時面か？下にコンテンツが流れるか？」を両方満たす場合のみ。

## 余白・寸法のリズム

- サイドバー: **セクション間 22px / セクション内 6–8px**。この緩急を守る。
- サイドバーの横長コントロールは**上下スリム**（カテゴリ行・表示切替=5px、select=5px、検索=6px の縦パディング）。ダイアログ既定（8px）をそのまま持ち込まない。横パディング12pxは23px軸を担うので変えない。
- **テキスト開始軸 23px**: sidebar pad 10px + border 1px + コントロール内 pad 12px。
  コントロールを持たない裸テキストは 13px インデントで軸を揃える。
- グリッド側コンテンツ pad: 24px 32px。

## モーション

- 150–260ms / ease-out（`--ease-out`・`--dur-*`）。出現は fade ＋ 4–6px rise（`corpusFadeIn` / `corpusPopIn`）。
- 入場アニメは「新規構築」時のみ＝初回ロード・フィルタ/検索/ソート/表示切替。**ミューテーション（ワークスペース/フォルダの追加解除・削除・グループ変更・タグ保存）とバックグラウンド更新（fs-watch・一括削除後の再読込）では再生しない**（keepLimit 再描画を使う。「画面がリフレッシュした」ように見える＝ユーザー指摘済み）。ページ送り（load-more）も再生しない。
- `prefers-reduced-motion: reduce` で必ず無効化する。

## 操作語彙

- **クリック＝トグル**（追加/解除）。3状態サイクルは廃止済み — 復活させない。
- **ドラッグ＝組合せの変更**（クエリビルダーのピルを「かつ/または」フィールド間で移動）。
- **ホバー＝最頻アクションだけ（カードは⚡と ℹ の2個まで）**。取り消し系アクションはホバー時に赤（控えめな赤）。
- **右クリック＝そのカードの全アクションの目次**（開く/タグ編集/フォルダ/ワークスペース/詳細/削除）。低頻度・破壊的・「選ぶ」系の操作はホバーに並べず右クリックメニューへ。ラベル＋アイコン付き行（fold-menu 様式）、破壊行は赤（`fm-danger`・srgb 混合）。ホバーアイコンを3個以上に増やす変更は飽和の再発＝却下。
- **フライアウト**＝サイドバー行の右に開く（カテゴリ→値）。値が8件超なら絞り込み入力を付ける。
- **モーダル**＝設定・確認のみ。中央寄せ＋backdrop。全データ削除などはキーワード入力ゲート必須。
- **インスペクタ**＝ℹ で右カラムに常設表示（非モーダル）。広い幅はインライン、狭い幅（<1280px）は**同じパネル**がスライドオーバーに（消えない・レイヤリングだけ変わる）。表示中は対象カードに tint リング。インライン時はカードクリックで内容差し替え（ダブルクリックでギャラリー）。**スライドオーバー時はパネル外（グリッド/カード）のクリックで閉じる**（クリックは消費＝同じ押下でカードは反応しない。ℹ だけは差し替えとして生きる）。Esc/× でも閉じる（上に乗るオーバーレイが先に Esc を消費）。
- **ピン＝ユーザー制御の常駐化**。頻度ベースの自動表示 UI は採用しない（認知負荷・却下済み）。
- **ワークスペース vs フォルダ**＝「使い捨ての一時トレイ」と「永続の棚」。ワークスペースは単一・ホバーボタンのワンクリックで放り込み（迷いゼロ）。フォルダは複数・名前付き・右クリック→ピッカーで追加先を選ぶ（明示）。**デフォルトフォルダは廃止**（ワンクリック自動追加は「1つしか入れられない」誤解と固さを生んだ）。アイコンは**ハンマー（lucide hammer のモノトーンSVG）**＝「作業場」のニュアンス（ユーザー選定。ブックマーク=お気に入り保存とズレ・⚡=不明瞭・トレイ=作業場感が弱い、で却下。ピンは「常駐化」で使用済みのため不可）。意味はホバーのツールチップ（「一時的な作業場」）で補足。**「in」状態の常駐表示はフォルダ同様却下**＝ホバー時のみ、入っている投稿はホバー赤（クリック＝外す予告）。中身を出す操作は**「空にする」＋確認ダイアログ**（「クリア」表記はフィルタ解除と誤読されるため却下。投稿自体は消えないことを必ず明記）。
- **連続タグ付け（旧称ウィザード）＝1画像（大）×1グループずつ**。UI文言では「ウィザード」「プルダウン」等の開発者語彙を使わない（一般的でない・ユーザー指摘）。全語彙の一覧スキャンも、隠して y/n で問う方式も両方却下済み（前者は認知ストレス、後者は recall を要求して答えられない）。「見せる量を1グループに区切る」が解。ステッパーで全グループが常に見え、ジャンプ可＝順序に意味を持たせない。レコメンド/自動候補は採用しない（不透明・不安定なものはユーザーが使わない。Corpus は決定的・ユーザー主導）。グループの表示/非表示はユーザー設定（常設の「非表示」チップから可逆）。
- ショートカット: `Alt+S`（拡張・キャプチャ）/ `Ctrl+A`（全選択）/ `/`・`Ctrl+K`（検索フォーカス）/ `Alt+←→`（フィルタ履歴）。
  グローバルショートカットは「入力中・オーバーレイ表示中は奪わない」ガードを必ず入れる（Ctrl+A ハンドラが雛形）。

## 状態の見せ方

- 件数バッジは tinted（カテゴリ行の右端）。
- アクティブな条件はクエリビルダーバー（常時表示）に集約。ゾーン内ラベル＋ピル間の演算子表示で
  「文として読み下せる」状態を保つ。ⓘ で説明ポップを出す。
- **空状態には必ず「次にやること」の導線**（CTAボタン or ショートカットヒント）を置く。
- フィルタ条件から外れるミューテーションは即座に画面から消さない（sticky-visible ＋トースト、次の画面更新まで残す）。
- フィルタ/表示状態の変更はブラウザ式の戻る/進む履歴に乗せる（表示密度・タイルサイズは対象外）。

## 却下済みデザイン（再提案しない）

- 「よく使うタグ」frecency 自動表示 → 認知負荷。**ピン留め**で代替。
- 「＋フィルタ」集約ポップオーバー → クリック数過多。**カテゴリ行＋フライアウト**で代替。
- ソリッド塗りのアクティブ状態 → チープ。**tint** で代替。
- タグチップの3状態サイクル（or→and→off） → 分かりづらい。**トグル＋ドラッグ**で代替。
