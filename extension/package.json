{
  "name": "hologram-extension",
  "private": true,
  "version": "1.1.0",
  "description": "Hologram Chrome extension (MV3), built by WXT",
  "overrides": {
    "brace-expansion": "^5.0.8"
  },
  "scripts": {
    "build": "npm run tokens && wxt build",
    "build:ext": "npm run tokens && wxt build",
    "dev": "npm run tokens && wxt",
    "dev:ext": "npm run tokens && wxt",
    "zip": "wxt zip",
    "typecheck": "wxt prepare && tsc --noEmit",
    "postinstall": "wxt prepare",
    "tokens": "node ../scripts/gen-extension-tokens.cts"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "typescript": "^7.0.2",
    "wxt": "^0.21.1"
  }
}
