{
  "name": "hologram-extension",
  "private": true,
  "version": "1.1.0",
  "description": "Hologram Chrome extension (MV3), built by WXT",
  "scripts": {
    "build": "wxt build",
    "build:ext": "wxt build",
    "dev": "wxt",
    "dev:ext": "wxt",
    "zip": "wxt zip",
    "typecheck": "wxt prepare && tsc --noEmit",
    "postinstall": "wxt prepare"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "typescript": "^7.0.2",
    "wxt": "^0.20.26"
  }
}
