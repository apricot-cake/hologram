# Eagle Info+ — Claude Code 作業メモ

> ⚠️ このリポジトリは**公開**されている。`docs/` 配下も含めて全て公開される。
>
> - **API キー・トークン・認証情報などの本物の秘密は、そもそも書かない** (サニタイズではなく省略する)。どうしても手元に残すなら git 管理外 (gitignore 済みの `.claude/` やローカルファイル) に置く。
> - **個人パス・実ライブラリ名などマシン固有の値は、書くときにサニタイズ (伏字・汎用化) してから書く。** 例: `C:\Users\apricot\...\資料.library` → `C:\Users\<name>\...\<lib>.library`、実件数 → 「約 9000 件」。技術的知見そのものは残し、個人を特定しうる部分だけ落とす。

## 知見・デバッグ情報の記録ルール

実装中に得た知見は **`docs/` 配下の markdown に書き出す**。チャットだけで完結させない。

- 既存トピックの追記なら該当ファイル (例: Eagle Plugin 関連は `docs/eagle-plugin-notes.md`) に追記
- 新トピックなら `docs/<topic>.md` を新規作成
- `docs/` は通常の追跡ディレクトリとして親リポにコミットされる。公開される前提で書く (秘密情報は上記の通り書かない)

## `docs/` について

`docs/` は以前 `eagle-info-plus-private` という別の private repo に分離していたが、メインリポに統合された。今は親リポ (`eagle-info-plus`) の一部として通常通り追跡・コミットする (`git add docs/`)。private repo は廃止。

### 書き出す対象

- **実機で確認した surprising な挙動** — 公式ドキュメントから素直に予測できないもの (silent な無視、戻り値の形が docs と違う、JSON.stringify の罠 等)
- **環境固有のメモ** — バージョン、ロケール、おおよその規模 (例「約 9000 件」) など。個人を特定しうる実パス・実ライブラリ名は**サニタイズ (伏字・汎用化) して書く** (例: `C:\Users\<name>\...`)
- **失敗したアプローチと原因** — 同じ落とし穴を避けるため
- **公式ドキュメントへのリンク** — 引き写しはしない、参照だけ残す

### 書き出さない対象

- **公式ドキュメントに書いてあることの複写** — 公式リンクで足りる。コピーは古くなる
- 一回だけのコマンド実行結果
- すぐに code に反映できる小さい修正
- チャットの会話ログ
