# React island bundles are generated by Vite (app/islands/build.mjs), which emits
# LF. Pin LF so Windows core.autocrlf=true doesn't smudge the checked-out bundles
# to CRLF and report every rebuild as a spurious change.
app/renderer/islands/** text eol=lf
