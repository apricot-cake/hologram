# Hologram スクリプト・テスト

## テストケース定義・進捗

- テストケース定義: `scripts/test-plan.md`（プラットフォーム × ページ種別の取得マトリクス）

## `scripts/` カタログ（俯瞰）

スクリプトの正本は `scripts/` 内の実ファイル（`node scripts/<name>.cts` で実行）。ここは逐一列挙でなく**種別の地図**に徹する＝何がどこにあるかだけ示し、各テストが具体的に何を検証するかは当該ファイル冒頭コメントを正本とする（網羅列挙は記載漏れで陳腐化するため置かない）。

### ユーティリティ

- `inject-dummy.cjs` — `dummy-` 始まりの条件網羅フィクスチャ（jpg＋色付きアバター＋DBのレコード）を既定保存先に生成。常設の検証フィクスチャとして残してよい（下の「行動ルール」参照）。electron 直起動なので拡張子は `.cjs`（理由はファイル冒頭・下記 make-icons と同じ）
- `gen-dummy-library.cts` — 規模を指定した合成ライブラリ（プレースホルダ画像＋`hologram.db`）を repo 外の任意フォルダへ生成（#175）。`bench-baseline.cts` の計測対象で、実データを使わずに件数・分布の偏りを再現できる
- `bench-baseline.cts` — 性能計測ハーネス（#293）。`<libraryDir>` とその `hologram.db` に対して cold/warm/差分書き込み/検索/ファセット/FTS/IPC相当を測り、環境ブロックごと JSON で出す。**数値は1回の記録の中でだけ比較可能**（マシンと負荷状態が同じである必要があるため）。#5 の before/after は同 Issue の 2026-07-23（旧経路）と 2026-07-28（DB経路）のコメント
- `backfill-metadata.cts`（欠損メタを保存URLから再取得してDBへ書き戻す・`--all`＝アバターも・`--avatars`＝アバターのみ。**単一ライターなのでアプリを閉じて実行**）／`make-icons.cjs`（`assets/icon-master.png` から全アイコンを再生成・手順は docs/build.md。electron 直起動のため `.cjs`＝`.cts` だと `require('electron')` が壊れる）

### キャプチャ／メタデータ検証

- 半自動フローの土台＝`test-select-posts.cts`（対象投稿の自動選別）→ `test-watch-verify.cts`（保存先監視＋API再照合・`--recent N` で一括点検・`--id <captureId>` で1件指定。DBは読み取り専用で開くのでアプリ起動中でも可）。**照合の判定規則そのものの単体は `verify-record.test.ts`**（ネットワークもDBも使わない）＝何を FAIL と呼ぶかがここに固定される。とくに①レコードが指す保存ファイルは `image` だけでなく `media[]` の原寸・ポスター・アバターまで全部見ること（#377 以降、投稿の原寸の家は `media[]` なので `image` だけを見ると1枚もディスクに無いレコードが PASS する）②**API に届かなかったことを届いたと読み違えないこと**（extractor はネットワークに出る前に URL から screenName を組むので、失敗した応答も screenName だけは持って返る＝それを「取れた」の根拠にすると、レコードの投稿者をそのレコード自身の url と突き合わせて PASS と印字する）の2点は、実行して目で見た人だけが気付ける類なのでテストが正本。`e2e-capture-test.cts` は WXT の本番出力を Playwright Chromium へ読み込む実サイトカナリア（ユーザーChrome・Alt+S 不要）。画面を出さない隔離実行はプラットフォーム名と `--headless` を渡す（例: `node scripts/e2e-capture-test.cts bluesky --headless`）。X はログインなしでは投稿DOMを出さないため、検証時だけ閉じた Chrome のプロファイルを `--user-data-dir` で渡す。ブラウザE2Eの初回実行前にリポジトリ直下で `npx playwright install chromium` を実行する。`npm run test:e2e-extension` は固定fixture・モックAPI・一時Native Messagingホスト・一時ライブラリで `content → background → native-host → ファイル着地` をネットワークなしに確認する。同じ土台の `npm run test:e2e-duplicate` は重複保存の警告（#34）＝同じ投稿を2度撮ると3択が出ること・「スキップ」で何も書かれないこと・「置換」の封筒が `replaces` に旧 captureId を載せることを見る（本物のブリッジが答えるので、保存済み判定がジャーナル越しに効いていることまで含む）。**応答が返らないときに保存が必ず終わること**（#507）は2本で見る＝`capture-timeout.test.ts`（jsdom・`npm test` 常時）がコンテンツスクリプト側＝バックグラウンドが一切答えない状況を作り、90秒の見張りで失敗表示に落ちること・チャネルが閉じたら待たずに落ちること・`capture.log` 行が出ること・重複問い合わせ（#34）が答えを得られなくても保存へ進むこと（fail open）を確認する。時間はテストが持つ手動クロックで進めるので実時間はかからない。`npm run test:e2e-timeout` は実 Chromium 側＝プラットフォームAPIのルートを fulfill も abort もせず握ったままにして、サービスワーカーのメタデータ予算（20秒）で保存が終わり `stage=metadata` の失敗が `capture.log` に残ることを見る。上限値そのものは `extension/utils/deadline.ts` が1箇所で持つ。**その終わり方が記録に残り、「起動しただけ」と読み違えられないこと**（#519）は同じ2層で見る＝`save-log.test.ts`（jsdom・`npm test` 常時。装置は `capture-timeout.test.ts` と共有の `lib-capture-rig.ts`）がやめた場合（Esc／重複警告の「やめる」）と止まった場合で違う行が出ること・ワーカーが黙る前に通過した段が最後の行に載ること・終わった保存に `cancel` を足さないことを確認し、`npm run test:e2e-save-log` が実 Chromium・実ブリッジで**3つを1回の実行で走らせて記録を並べて印字する**（①UIを開いて閉じた ②保存が途中で止まった ③保存が普通に終わった）＝この印字が「区別できる」ことの現物で、行がホストのプロセスを通ってディスクへ着くことまで含む。`stage`／`phase` の値の意味は docs/build.md「保存が失敗した時に見るログ」が正。**ページ内 UI がホストページから隔離されていること**（#44・#310）は `npm run test:e2e-hostile-css`＝`* { all: unset !important }` 相当の敵対 CSS と、拡張のクラス名を名乗る要素を置いたページの上で、ドロップゾーン（固定レイヤーの共有 ShadowRoot）と写真の角のコントロール（各自の小さな ShadowRoot・#310）が設計どおりの寸法・位置・塗り・輪郭・影のままであること、逆にそのページ自身の要素へ拡張のスタイルが当たっていないことを数値で見る。同じ台で `style-src 'self'` も返す＝構築済みシートは CSP の検査対象ではないので `--hologram-surface` が解決されること（#270 の実測）まで一緒に押さえる。**⚠️敵対 CSS は外部シート（`<link>`）で配る**＝`<style>` に書くと `style-src` がそれごと殺し、敵対 CSS が一度も当たらないまま全部緑になる（初版がこの状態だった・2026-07-30 に #310 で発見）。空振り防止に「ページ自身の `.surface` がマゼンタか」を先頭で検査している。ここが外れても他のテストは全部緑のまま通るので、境界の有無はこの1本でしか分からない。API取得の単体は `metadata-correctness.test.ts`／`metadata-origin.test.ts`／`mastodon-url.test.ts`／`extractor-hashtags.test.ts`。最後の1本（#177）は**5PFを1ファイルに並べる**＝各PFの置き場（X=`entities.hashtags[].text`・Bluesky=tag ファセット＋`record.tags[]`・Misskey=`note.tags[]`・Mastodon=`tags[].name`・pixiv=`tags.tags[].tag`）から取れることに加えて、**入る形が全PFで同じ**こと（先頭の `#` を持たない素のタグ・投稿内の重複なし）を1つの expect で突き合わせる＝ここが揃わないと同じタグがファセットで2つに割れるが、PFごとに別ファイルで見ている限りどのファイルも単体では緑のままになる。Native Messaging の失敗分類と日英の案内文は `save-error-i18n.test.ts`、`extension/utils/extractor/` 各サイトモジュールのDOM抽出は `content-fixtures.test.ts`（fixture=`scripts/fixtures/content/`・jsdom・`npm test` 常時）で確認する。サイト別モジュールを束ねる**登録簿そのものの不変条件**（platform の一意性・DOM 相と API 相が同じ platform を名乗ること・インスタンス型サイトが並びの後ろに来ること・manifest の match/host_permissions が登録簿から組み上がること）は `extractor-registry.test.ts`（#212）。TL上のオーバーレイ（#54/#94/#309/#334）は `overlay.test.ts`（WXTの生成した resident bundle をjsdomで実行）と `bridge-query.test.ts` に分かれる。前者は照会のバッチ化・マークの3値設定・ホバー保存ボタンの表示・`imageDragged` 経路の再利用・失敗の表示と復帰・投稿DOM非変更を確認する。保存済み判定は投稿単位でなく**画像単位**（#334）で、その3層＝画像 URL の同一性規則が `media-identity.test.ts`（extractor の `mediaKey`／`mediaKeyOf`＝サムネ/原寸/`name=orig`/`@jpeg` の表記ゆれを畳む唯一の規則）、DB からスナップショットを組む側が `saved-index.test.ts`、3情報源を合流して答える側が `bridge-query.test.ts`。**ゴミ箱に現物が残っている投稿の告知（#158）は同じ3本＋実起動1本**＝スナップショットの `trashed` マップを組む側が `saved-index.test.ts`（保存済みが勝つ・同じ投稿を2回削除したら新しい日付・postKey が作れない記録は載らない）、それを読んで別マップで返す側が `bridge-query.test.ts`（`results` は null のまま・`trashed` を持たない古いスナップショットでも落ちない・壊れた値は型を通す）、判定が拡張へ届く配線が `background-wiring.test.ts`、バナーの文言と2択（置換を出さない）が `capture-overlay.test.ts` と `drag-zone.test.ts`。**ゴミ箱操作がスナップショットを書き直すことだけは実起動でしか出ない**＝`test-app-trash-notice.cts`。各PFの実DOMにセレクタが今も当たるかはe2eカナリアと実機確認の領分。**取得APIの応答構造そのものの変化は `schema-canary.cts`**（#191）＝`scripts/canary/samples.json` に並べた投稿タイプ別の公開サンプルを取得し、値を捨てた「フィールドパス→型」の木を `scripts/canary/snapshots/<platform>.json` と突き合わせる。消失は2回連続で初めて警報（サーバー側のA/Bや条件付きフィールドで毎回鳴らないように）、新規出現は即報告。終了コードは 0=変化なし / 1=消失の確定 / 2=警報は無いがサンプルが取得できない（＝サンプルの差し替えが要る）。応答は `fetchPostMetadata` が #292 で原本ごと返すようになったのでそれを読む＝拡張が実際に叩く経路そのものを観測する。鳴った後に実物を見るのは `--dry-run` ではなく `--payloads`（保存済みの原本と基準を突き合わせる・DBは読み取り専用）。判定部の単体は `schema-canary.test.ts`。X ブックマークの一括取込（#362・追いかけ型）は `bulk-capture.test.ts`（WXTが生成した capture bundle をjsdomで実行、要 `extension` で `npm run build:ext` 済み）で、機械が自らスクロールしないこと・保存済みのスキップ・見送り（撮り逃し）の検知と再開・進捗/中断バナーの文言を確認する。**投稿を1つも取得できなかった保存が「保存済み」にならないこと**（#492）は両端で押さえる＝書く側が `save-post-empty.test.ts`（`handleSavePost` が殻レコードを断り、エンベロープもバッジのジャーナルも残さない）、答える側が `saved-index.test.ts`（中身を持たない投稿はスナップショットに載らない）。判定規則そのもの（`recordHoldsContent`）の網羅は `post-record.test.ts` で、この3本がずれると殻レコードにバッジが点いたまま残る。**拡張⇄ホストのメッセージ契約は `host-protocol.test.ts`**（#400）＝拡張とホストは別の TS プロジェクトなので、型検査は「同じ宣言を import しているか」しか見られない。ここは `startBackground()` を実際に動かして**線に載ったメッセージそのもの**を共有 parse（`native-host/protocol.mts`）に通す＝フィールドを片側だけ改名すると型検査が通ってもこれが落ちる。captureId が保存フォルダから出られない形だけを通ること（パス区切り・`..` の拒否）も、規則を持つ側であるこの契約で見る。**プロトコル版のハンドシェイク**（#205）も同じファイルで、①比較が整数比較だけであること ②版を名乗らない返信が `host-old` になること ③**ずれていても保存が完走し、結果に更新案内だけが乗ること**を見る。⚠️③が本体＝「検知して止める」に転ぶと投稿を落とす。刻印が**実際に線へ出ている**ことは `bridge.test.ts` が受け持つ（ホストのプロセスを起こして stdout のフレームを読む＝刻印はハンドラでなく返信の出口にあるので、単体では見えない）。画面まで届くかは `capture-overlay.test.ts` と `drag-zone.test.ts`（保存の出口が3本あるので、1本でしか案内しないと普段その経路を使わない人には届かない）。

### スモーク／退行・セキュリティ・正しさ

- 実Electron main は `HOLOGRAM_SMOKE` harness で起動。`scripts/test-*.cts` 群がカバーする領域＝ブリッジ・原寸メディア・IPC・ハッシュタグ・自動更新・ユーザー/インスタンス・タグ用語帳・クエリビルダー（text 葉化／保存検索）・クエリエンジン純ユニット（`query.test.ts`＝`src/renderer/src/services/query.ts` の述語/ツリー評価/日付境界/ファセット・ドメイン＝改訂④）・保存先移行エンジン純ユニット（`migrate.test.ts`＝`src/main/lib-migrate.ts` の差分追いコピー/検証付き削除/落ち穂拾いスイープ/crash-safe順序）・import 重複検出（`test-app-import-dedup`＝URL＋eagleName/capturedAt/サイズ複合キー・.trash 復活防止）・ゴミ箱の告知（`test-app-trash-notice`＝#158。削除→復元→削除→ゴミ箱を空にする、を1回の起動で順に踏んで、ブリッジの答えが `saved,trashed,saved,trashed,none` と動くことを見る。**見ているのは配線**＝#158 まで `ipc-trash.ts` は索引を書き直しておらず、削除しても「保存済み」と答え続けていた。ゴミ箱マップと保存済みマップが同じ鍵を共有しないことも同時に確認する＝共有するとゴミ箱の投稿でTLバッジが点く）・タブ・カード脚注ゲート（`test-app-cardfoot`＝エンゲージメント数/取込日はソート・フィルタが関係するときだけ表示）・クリック操作モデル（`test-app-click-model`＝#143 の統一ジェスチャを実レンダラで検証: プレーンクリック=単一選択+インスペクタ・Ctrl/Shift 選択・インスペクタサムネ→クイックビュー・投稿者ダブルクリック=ドリルイン・ℹ/○ホバー部品の撤去・矢印キーでの選択移動とインスペクタ追従・端でのクランプ）・グリッド余白のジェスチャ（`test-app-marquee`＝#484 のマーキー選択と #242 の余白クリック。同じ押下の二つの結末なので1本で見る。判定の幾何そのものは `marquee.test.ts`（純ユニット）が持ち、こちらは実レンダラでしか確かめられない側を見る＝**期待値を live な DOM の矩形から導いて突き合わせる**（仮想グリッドで唯一怖いのは「モデルの答えとカードの実位置がずれる」ことなので、そこを直接測る）・余白からの mousedown が起点になっている・Ctrl は足す/素は置き換える・Esc でドラッグ前へ戻る・カードの上からのドラッグは矩形にならない・ドラッグにならなかった押し離しは選択を解除しインスペクタをプレースホルダへ戻す・Ctrl/Shift 併用の余白クリックは何も変えない・最終行より下の空きも余白として効く。**自動スクロールと操作感は対象外**＝合成イベントはフレームを瞬時に飛ばすうえ、隠しウィンドウは rAF を強く絞る（各パスが通るのはマウスアップ時の最終同期パスのおかげ）。実マウス確認に残す）・インスペクタ内インラインタグ編集（`test-app-inspector-tags`＝P2⑦ でタグ編集がポップオーバーからインスペクタへ移った後の契約: 自由入力+Enter で追加・チップの✕で削除・どちらもDBまで反映・候補popupが未取込のソースタグとライブラリ語彙を出す・タグpopup を開いたままの Esc でパネルごと閉じない・タグ入力中の矢印キーはキャレット操作で選択を動かさない）・俯瞰ズーム（`test-app-overview-zoom`＝#141 の Ctrl+ホイール1ノッチ・下限48pxまでのズームアウト・96px 未満での情報オーバーレイ自動 OFF（「タイルに情報を表示」の pref は書き換えない）・停止後の確定と永続化、および #282 のアンカー維持＝カーソル下の投稿がズーム後も画面の同じ高さに残る。**位置合わせの幾何そのものは `zoom-anchor.test.ts`（純ユニット）が持ち**、こちらは実レンダラでしか出ない側＝再レイアウトを跨いだカードの実位置を測る。仮想グリッドを scrollTop 直代入で大きく飛ばすと描画窓が作り直されるまで画面が空に見えるので、**セルが実際に見えるまで待ってから測る**（固定 sleep では3回中2回誤検出した））・画像ビューのツールバー（`test-app-image-zoom`＝#150。ズーム − / % / ＋ とフィット⇄原寸、`Ctrl+0`／`Ctrl+1`。**倍率の算術そのものは `image-zoom.test.ts`（純ユニット）が持ち**、こちらは React のツリーとストアが全部つながらないと決まらない側＝ツールバーがトップの帯に出ているか・グリッドタブでは出ないか・動画スライドでズーム系が disabled になり次の画像スライドで生き返るか。**ホイールとダブルクリックの回帰もここ**＝ボタンと同じ関数・同じ累積ターゲットを共用させたので、速いホイール4ノッチが 1.25^4 ぶん効くこと（#134）とダブルクリックのフィット切替を並べて見る）・うごイラ再生（`test-app-ugoira`＝#119 St3。pixiv の zip をレンダラが IPC 経由で開き canvas へコマ送りする経路。3コマを別色にした zip を実 Electron で再生し、画素が実際に変わること・一時停止で止まり再生で再び進むことを見る。**`fetch('asset://…')` は使えない**＝file:// の文書から独自スキームへのクロスオリジン fetch を Chromium が拒む・CSP では覆せない）・フォルダ・自動バックアップ・Zip-Slip/zip爆弾(展開上限)/SSRF・メタ正しさ・テーマトークン/コントラストのパリティ・送り出し（#132＝3層に分けて検証: `library-files.test.ts`＝main のファイル名ガード/欠損除外の純ユニット／`records.test.ts` の `dragFilesOf`＝「選択内を掴めば選択全体・選択外なら単一」の Explorer 規則の純ユニット／`test-app-drag-out`＝実レンダラでの dragstart 配線＝カード画像で preventDefault・選択の置換・本文からのドラッグは素通し／`test-app-copy-image`＝実 Electron で nativeImage が読めない形式を拒否しクリップボードを空で潰さないこと）。**OS ドラッグそのもの（startDrag→Explorer の handshake）だけは自動化対象外**＝`window.hologram` は contextBridge が deep-freeze していて IPC をスパイできず、ドロップ先も OS の領分。実マウス確認に残す。
- **`asset://` のスクリプト封じ（#215・[ADR 0012](decisions/0012-asset-documents-are-raster-only.md)）は `test-app-asset-csp` が3層を別々に測る**＝①`open-image-window` が SVG を拒む ②`will-navigate` が SVG へのトップレベル遷移を拒む ③それでも文書が立った時に CSP がスクリプトを殺す。**③だけは CDP でしか測れない**＝①②を塞いだ後はアプリ内のどの経路からも SVG 文書へ到達できず、「到達できないから安全」で終えると入口が1つ増えた日に何も守っていなかったと分かる。そこでデバッガから viewer 窓を直接 SVG へ飛ばし、CSP だけが残った状態を作る。**証拠の取り方は「手元の HTTP サーバへビーコンが1本も来ないこと」**＝仕込んだ SVG は走ったこと自体と隣のライブラリファイルを読めたことをそれぞれ投げる。**空振りでないことは確認済み**＝CSP を外すと `/ran-script`・`/ran-onload` に加えて隣のファイルの中身が実際に外部へ飛ぶ（2026-07-29 実測）。退行側は同じ harness が `<img>` 原寸／サムネ（`?w=`）／SVG を絵として／CSS background／単独ウィンドウのラスタ描画まで見る
- **「タイプ→候補」の供給源が1つであることは `command-registry.test.ts` と `command-corpus.test.ts`**（#28・[ADR 0016](decisions/0016-one-candidate-engine-three-faces.md)）＝前者がレジストリの意味論（セクションの束ね方・完全一致>前方一致>部分一致>あいまいの並び・面ごとの `sections`/`limit`・開閉状態・`runEntry` が閉じてから実行すること）、後者がエントリ生成（タグは SNS 投稿のみ集計・投稿者は displayName 優先で screenName へフォールバック・フォルダはパス表示・モードで宛先が変わるコマンド）。**検索ボックスとパレットが同じ供給から出ていることは後者が両方の面の指定を並べて突き合わせる**＝ここが外れても両方の面は単体では正しく見えるので、顔ぶれのズレはこの1本でしか分からない。
- **選択テキストの右クリックは `selection-menu.test.ts`**（#167）＝行の順番と act の振り分け、Google 検索 URL の組み立て（切替設定を作らない代わりに1箇所へ寄せた seam）、そして**出し分け**を見る。中心は「右クリックが選択の上で起きたか」の判定で、ここは**素直に書くと主経路がまるごと死ぬ**＝右クリックはテキストノードではなくそれを載せている要素に落ちるが、その要素は Range から見て「含まれても部分的に含まれてもいない」（両端の共通祖先だから）ので `Selection.containsNode(el, true)` は false を返す。`Range.intersectsNode` が正しい問いで、jsdom の実 Selection でそこを固定してある。
- **拡張のデザイントークンは `extension-tokens.test.ts`**（#270）＝アプリの `globals.css` から生成した `extension/utils/tokens.generated.*` が最新か・拡張のコードが実在するトークンだけを参照しているか・色のベタ書きが復活していないか・**両テーマ × 参照4下地（純黒 / X dim / pixiv dark / 白）でコントラストが足りているか**を見る。4下地を固定するのは、拡張の面がアプリの選んだ背景ではなく任意のページの上に乗るため＝アプリ内なら成立する「サーフェス色に近い細い境界線」がそのままでは成立しない。アプリ自身のトークン側のガードは `token-parity.test.ts`／`contrast-parity.test.ts` で、こちらとは別物。
- **生成された manifest とそれを前提に書いたコードの突合は `ext-consistency.test.ts`**（#130）＝WXT 移行（#198）と extractor 登録簿（#212）で manifest は生成物になったので「手書きの対応表がずれる」型の事故は構造的に消えたが、**生成物とコードの間の約束**は型でも lint でも捕まらず実機でしか分からない。ここが見るのは①生成された match / host_permissions が登録簿の宣言そのままか ②manifest が名指しするファイルと、コードが文字列で名指しして注入するファイル（`files: ['capture.js']`＝リネームすると Alt+S だけが静かに死ぬ）が出力に実在するか ③`commands` と待ち受けのコマンド名が一致するか ④`key` から決まる拡張IDを、それを許可する側（e2e ハーネスが組む Native Messaging の `allowed_origins`）も同じ値で持っているか ⑤`__MSG_*` と `getMessage` のキーが実在する文言か（**使う側との突合はここだけ**＝`i18n-parity.test.ts` は日英テーブル同士しか見ない）。⑤は逆向き＝**どこからも呼ばれないキーが残っていること**も落とす。
- **外から来たレコードの「形」を信じてよい境界は `hostile-record-shape.test.ts`**（#324）＝レンダラは `tags` を配列・`title` を文字列として読むので、形の違うレコードが1件でも届くと描画が例外になり、React のルートが1本な以上ツリー全体（グリッド・サイドバー・インスペクタ・設定・ゴミ箱）が同時に消える。そこで**外部由来のレコードが入ってくる3経路を1本で並べて見る**＝①ZIP インポート→DB→読み出し（`writePost` が `normalizePostRecord` を必ず通すので既に閉じている＝正規化を外したら落ちる退行テスト）②`posts.hashtags`（JSON 文字列カラム。この読みは投稿一覧の全件なので、素の `JSON.parse` だと1行の値でライブラリ全体が読めなくなる）③`.trash/<captureId>.json`（レンダラがディスクの JSON をそのまま受け取る唯一の場所＝敵対的な完全形式 ZIP はここへ任意の JSON を置ける。zip-slip の検査はエントリ名だけで中身の形は見ない）。**③だけが実際に再現した境界**で、`listTrashRecords`（`lib-trash-capture.ts`）がその検査を持つ。描画側の保険（`app/ErrorBoundary.tsx`）はここでは測れない＝正規化が想定していないフィールドで落ちた時の受け皿なので、実機で見る領分。
- **保存フォルダの封じ込め規則は `save-folder-path.test.ts`**（#267）＝ライブラリのファイル名が解決してよい先を1箇所（`lib-save-folder-path.ts`）で決めており、`asset://` のハンドラも受信箱の取り込みも整合スキャンもここを通る。見るのは**通る形と通らない形を同じファイルに並べる**こと＝ルート直下・`avatars/<file>`・`.trash/<file>` の3形が通り、2階層目・許可していないサブフォルダ・絶対パス・`..` の各綴りが保存フォルダの外を指さないこと。許可を広げる変更は「通る形」のテストだけを緑にして通過できるので、**広げすぎていないことの証明を同居させる**のが本体。加えて #267 の原因は独立した2つ（ゴミ箱のレコードが名乗るパスと、封じ込めの許可リスト）で片方だけ直しても画は出ないので、`listTrashRecords` が返した名前が実ファイルまで解決することを最後に噛み合わせる。
- **contextBridge の境界（window.hologram）に生の IPC イベントが漏れていないことは `preload-bridge.test.ts`**（#383）＝`electron` を丸ごと差し替えて preload を素の Node で読み込み、公開されている `on*` を**全部走査**して、コールバックが `IpcRendererEvent` を受け取らないこと・渡る引数が payload 1つ以内であることを見る。走査にしているのは**この漏れが無言で、しかも正しく動く**から＝`ipcRenderer.on(ch, cb)` と素通しで書いてもレンダラー側は第1引数を `_e` として捨てるだけなので、目視でも実機でも気付けない（#383 で実際に3本が越えていた）。新しい通知 API を素通しで足した瞬間にここだけが赤くなる。
- **プリファレンスが本当に config.json へ着くことは `inspector-pref.test.ts`**（#391）＝`electron` を差し替えて本物の `ipc-config.ts` を登録し、その `set-pref` / `get-prefs` を `window.hologram` のスタブへ配線して、レンダラー側の保存モジュール（`inspector-panel.ts`）と main の許可キー（`PREF_KEYS`）を**1本の線でつなげて**見る。片端だけ見ても捕まらないから＝`set-pref` は許可キーに無いキーを `{ok:false}` で捨て、呼び出し側はその返り値を読まないので、**キー名の取りこぼしは無言で、しかも保存できているように見える**（`inspectorOpen` は数ヶ月そうなっていて、config.json と突き合わせるはずの `load()` は dead code だった）。同時に起動時の勝ち負け（config.json がキャッシュに勝つ・未設定は「一度も切り替えていない」・起動途中のユーザー操作は上書きされない）も固定する。新しいプリファレンスを足す時はこの形を写す。
- **周辺UIの一括表示トグルは `panels-pref.test.ts`**（#245）＝上の形を写した `panelsHidden` の往復に加えて、**一括状態が「2つのパネルの状態を書き換えず覆うマスク」であること自体**を固定する。ここが実装の選択そのものを縛っている＝「隠した瞬間の組み合わせをスナップショットに退避する」実装でも普段の操作は同じに見えるが、一括状態は config.json に残る一方でメモリ上のスナップショットは再起動で消えるので、**隠したまま終了 → 起動 → 戻す**で組み合わせを見失う。その一連をそのままテストにしてある。相方の `Ctrl+B`（サイドバー単体）は別ファイル（`components/ui/sidebar.tsx` の `SidebarProvider`）にいて、2つのハンドラが同じ物理キーを Shift の有無だけで分け合う形なので、その境界（Shift 無しには手を出さない・Caps Lock で大小が入れ替わっても同じ chord は同じ意味）も併せて見る。
- **クリップボード取込は `clipboard-intake.test.ts`**（#85）＝**実クリップボードを絶対に読まない**のがこのスイートの設計。読んでしまうと、緑だったのが「正しく動いた」からなのか「たまたま画像がコピーされていた」からなのか区別できず、走らせた人の手元とCI・並行セッションを巻き込む。差し替えるのは `electron` の `clipboard`（`availableFormats`/`readImage`）だけで、保存先への書き込み・DB への書き込み・カード実寸の計測は製品コードがそのまま走る（テンポラリの保存先と `hologram.db` を本当に作る）。見るのは #85 の受け入れ条件そのもの＝①**入力欄（INPUT/TEXTAREA/contentEditable）とオーバーレイ表示中に発火しないこと**（Ctrl+V は貼り付けのキーなので、これが機能の成立条件）②画像を持たないクリップボードがエラーでなくトーストで終わること ③貼った画像がレコードになり `posts-changed` が飛ぶこと（アプリ内書き込みは取込キューのイベントを残さない＝レンダラへ届く線はこれ1本）。共通ヘルパ `lib-local-intake.ts` の側も同じファイルで見る＝#84・#234 が乗る時にレコードの形が入口ごとにずれないため
- **復旧系は 2026-06-23 ライブラリ消失対策の多重防御**＝`test-app-recovery`（冗長ポインタ→config 復元）・`backup-guard.test.ts`（prune 安全弁）・`config-recovery.test.ts`（degraded 時の clear-all 拒否）。安全弁の意図はこの3本を正本とする。
- **一括実行**: `npm test`＝Vitest（`vitest.config.ts`）で Electron 不要の純ユニットを回す。**登録は glob**＝`scripts/**/*.test.ts` に置けば自動で拾われる（一覧を手で足す場所は無い）。**拡張のビルドは `npm test` 自身が面倒を見る**（#130・`scripts/vitest.global-setup.ts`）＝バンドルを読むスイート（`overlay` / `drag-zone` / `capture-*` / `bulk-capture` / `ext-consistency`）が古い `extension/.output` を読まないよう、ソースのほうが新しい（または出力が無い）ときだけ走り出す前に `build:ext` を1回回す＝**手で `npm run build:ext` してから `npm test` する必要はもう無い**。`wxt dev` が動いている間は出力がソースより新しいので何も起きない＝日常の Chrome が読んでいる dev ビルドを潰さない。**除外の理由は設定にコメントで書く**＝ネットワークが要る取り込み CLI（`test-metadata.cts`／`test-select-posts.cts`／`test-watch-verify.cts`）と Electron が要るアプリハーネス（`test-app-*.cts`）は `.cts` のままで、この glob に入らない。
  - 型検査は `npm run typecheck`（`scripts/typecheck.cts`）＝app renderer（`tsconfig.web.json`）・app main+preload（`tsconfig.node.json`）・native-host・extension・scripts・e2e の6プロジェクトの `tsc --noEmit`。テストではないので Vitest 管轄外。3つまとめて回すのは `npm run check`（lint→typecheck→test）＝マージ前のゲート。
  - 開発中は `npx vitest`（watch）・1本だけなら `npx vitest run scripts/query.test.ts`・カバレッジは `npx vitest run --coverage`。
  - **サーバー側で自動で回る範囲**（#15）: push ごとに `.github/workflows/ci.yml`（Linux）が `npm run check` を回す。実 Electron 起動系（`test-app-*.cts`・`e2e/flows`）と拡張のブラウザE2E（`e2e-extension-offline`／`e2e-extension-duplicate`／`e2e-extension-timeout`／`e2e-extension-save-log`／`e2e-overlay-visual`／`e2e-overlay-flicker`）は `.github/workflows/app-tests.yml`（Windows）が1日1回だけ回す＝**push では走らない**ので、触った直後の確認は手元で行う（手動起動は `gh workflow run app-tests.yml`）。実サイトカナリア（`e2e-capture-test.cts`）はどちらにも載っていない＝X がログインなしでは投稿DOMを出さないため、ランナー上では常にログイン壁を報告するだけになる
  - **実起動系ハーネスの言語は `ja` 固定**（`HOLOGRAM_SMOKE` 時に main が `--lang` を立てる。`HOLOGRAM_LANG` を渡せば SMOKE 以外でも効く＝Playwright 層はこちらを使う）＝ハーネスは日本語ラベルで部品を探すので、言語がマシン任せだと英語UIのランナーで「部品が無い」ように見える赤になる（2026-07-28・#15 第2段で en-US ランナーが実際にそうなった）
  - アプリ実起動系（`test-app-*.cts`）は含まれないので `node scripts/run-app-tests.cts` で一括実行する（1本≈10秒と重い＝節目で回す。renderer 再構築後の「`npm test` では見えない無音の赤」をここで検出する。引数でサフィックス指定のサブセット実行可）。
- **画面 E2E とビジュアルリグレッション（#14）＝ `npm run test:e2e`（Playwright・`playwright.config.ts` と `e2e/`）**。上の `test-app-*` 層との違いは**入力が本物**であること＝隠しウィンドウへ合成イベントを投げるのではなく、見えているウィンドウを実ポインタ・実キーで駆動する。これでしか捕まらないのが「pointer-events・重なり・レイアウト崩れでクリックが届かない」型と、見た目そのものの退行。プロジェクトは2つで、フロー（`e2e/flows`）は CI でも回り、スクリーンショット（`e2e/visual`）は**この開発機だけ**で回す＝正解画像はローカル基準（#14・2026-07-29 決定）。撮り直しは `npm run test:e2e:update`。前提・固定している変動要因・撮る面を絞っている理由は `e2e/README.md`。
  - **`npm test` にも `run-app-tests` にも入れない**＝別ランナー・節目実行。実行前に `npm run build --workspace=app` が要る（無いと起動を拒否される）。
  - フォルダのドラッグ＆ドロップ（#41）はもとは `scripts/test-app-folder-dnd.cts` だったが、同じ層なので `e2e/flows/folder-dnd.spec.ts` へ移した（二重に回さないため）。
  - 削除→ゴミ箱→復元（`e2e/flows/trash.spec.ts`）は**この層でしか見られない**＝両端が別の面にいる（選択バーで消し、左ナビの「ゴミ箱」で戻す・#268）ので、片方だけを駆動しても2つが今も噛み合っていることは分からない。ゴミ箱側の状態モデル（並び・グループ化・選択・一括適用・確認の有無）は純ユニットの `trash-view.test.ts` が持つので、こちらは**面と面のつながり**だけを見る＝ナビの項目が0件でも在ること・削除でバッジが立つこと・カードを選んで初めて「復元」が押せること・戻したらライブラリのグリッドに現れバッジが消えること。
- **ホバー保存の描画回帰**: `npm run test:overlay-visual`＝使い捨てPlaywright Chromiumへ本番ビルドの拡張を読み込み、X形式の画面で失敗バナーの上中央配置・角の再試行表示との併存、ボタンと画像のスクロール位置、固定ヘッダー/モーダルの重なりを実ブラウザで検査する（重なりの判定点は**ポインタ**＝画像の上端がヘッダーに潜ってもカーソルが画像上ならボタンは残り、カーソル自体がヘッダーへ乗ったら消える）。ホバー配置や重なりを変えたら必ず実行する。
- **ホバー保存のちらつき回帰**: `npm run test:overlay-flicker`＝X/Bluesky/pixiv 形状の fixture（`scripts/fixtures/overlay/`・#338 の「画像を覆う兄弟オーバーレイ」込み）上で、ホバー表示・同じ画像上での往復ホイール（jiggle-scroll＝カーソルが画像上にある間はボタンを外さない）・ホバー中の要素差し替え（re-render＝仮想スクロールの作り直しでもボタンを新要素へ引き継ぐ）・静止ポインタのホイールスクロール・ポインタ微動つきスクロールを駆動し、オーバーレイの付け外しと ページ要素への style 書き込みをタイムライン記録（`lib-overlay-e2e.cts` のレコーダー）で閾値判定する。ちらつき＝時間軸上の再マウント反復なので before/after 検査（overlay-visual）では見えない領域を受け持つ。オーバーレイのホバー・スクロール挙動を触ったら必ず実行。修正ループ中は `node scripts/e2e-overlay-flicker.cts x --verbose` のように単一PF指定＋タイムライン出力で回すと速い（ビルド済み拡張が前提）。ブラウザ起動は全E2E共通の `lib-extension-e2e.cts`、fixture配信とレコーダーは `lib-overlay-e2e.cts` が受け持つ。

## キャプチャテスト手順（半自動フロー）

1. `node scripts/test-select-posts.cts` — テスト対象投稿を公開APIから自動選別（セルごとのURL・アクション・期待値のシートを出力）
2. `node scripts/test-watch-verify.cts` — ライブラリの監視を開始（キャプチャごとにAPI再照合し PASS/FAIL を出力。DBを読み取り専用でポーリングするのでアプリ起動中でも可）
3. claude が in chrome でシートのURLを開く → ユーザーが Alt+S → クリック（またはドラッグ）
4. 次のセルに進む（過去分の一括点検は `node scripts/test-watch-verify.cts --recent N`）

### 注意（行動ルール）

- 同じセルを手で繰り返し叩かない。どこまでカバー済みかは `e2e-capture-test.cts` の実行結果で確かめる
- 1つの投稿で複数のケースをカバーできる場合はまとめて記録（例: A-1b と A-1h）
- **手元にないデータはダミーで補ってよい（区別必須）**: 既存ライブラリに無いデータ（例: アバター画像の無い投稿者）を実機検証したいときは、**区別がつくダミーを保存先に追加してよい**。区別の規約＝captureId とコンパニオン（画像/アバター）のファイル名を `dummy-` で始める（検索・一括掃除で実データと確実に分離できる）。`inject-dummy.cjs` がこの規約のダミー（アバター付き・条件網羅）を生成する。**常設の検証フィクスチャとして残してよい**＝再注入の手間が省け、全PF/種別/メディア/反応域/アバターを常に網羅するので普段使い中の退行にも気づける（ユーザー方針 2026-06-21）。データモデルが変わって古びたら `inject-dummy.cjs` を再実行して入れ替える。不要になれば保存先の `dummy-*` を削除すれば fs-watch が一覧から落とす。
