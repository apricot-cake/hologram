# バックログ（未着手の残タスク・唯一の作業トラッカー）

完了は git が記録するので**完了項目は残さず削除**。実装済み機能・構成・検証手順は CLAUDE.md 他節＋メモリ `corpus-verify-notes` が真実源（重複させない）。

## 作者まわり（クラスタ）
- **著者プロフィールの表示**: インスペクタに実装済み＝フォロワー数・登録日のテキスト行＋投稿者名の横に円形アバター。アバターはオフライン保存方式（DL ロジックは `native-host/media-download.js` に共有化＝capture/import/backfill で同一の SSRFガード/上限。`avatarFile` を psimg:// で表示。pixiv アバターは Referer ゲート対応済み。X/pixiv のフォロワー/作成日は公開APIに無く graceful 隠し）。取込(import-posts)・backfill(`--all`/再取得時)・既存データ(`backfill-metadata.js --avatars`＝API無しでアバターのみDL)の各経路にアバターDLを追加済み。**残**: 投稿者ビューにもアバター表示済み（実装は下記）だが、既存ライブラリは未backfillで `avatarFile` が無くモノグラム表示＝個人作業で `backfill-metadata.js --avatars` を流すと実画像に。
- **投稿者ビュー（実装済み 2026-06-18）**: コンテンツ上部の「投稿/投稿者」モード切替トグル（`browseMode`）で投稿者グリッドへ。カード=アバター/モノグラム＋投稿者名＋@＋PF＋投稿数。クリック=インスペクタ（投稿者プロフィール）／ダブルクリック=投稿モードでその投稿者の `user` フィルタ。投稿者モードのサイドバーは並び順（投稿数/名前/最新投稿）＋プラットフォーム絞り＋名前付きフォルダ＋検索。カードのホバーはライブラリと同じ3ボタン（🏷タグ・🔖コレクション・ℹ情報）。名前付きフォルダ＝サイドバーで作成/削除/リネーム/絞り込み/**D&D 並べ替え**・割当はインスペクタのチップ＋**カード右クリックのコンテキストメニュー**（`poster-folders.json`）。詳細は architecture.md「ビューア機能」。**残**: 作品タググループを第一級概念へ。
- **同一投稿者がSNS別にばらける→名寄せ（設計調査済み 2026-06-21）**: 同じ人でもプラットフォームが違うと別グループになる。手動で紐づける＋自動で同一人物を提案（名寄せ候補）。呼称は「投稿者」確定（DESIGN.md「用語」）。
  - **データモデル**: `<saveFolder>/poster-aliases.json`＝`{groups:[{id,primary,members:[posterKey]}]}`（poster-folders.json と同型＋代表キー primary）。posterKey も投稿サイドカーも壊さず別レイヤーで束ね、タグ/フォルダは「保存は個別キー・表示は実行時合算」＝可逆・非破壊。5点セット（get/set-poster-aliases IPC・preload・INTERNAL_FILES・clear-all スキップ・`mergePosterAlias`）は poster-tags.json が雛形。マージは union-find（1キー1グループ・primary ローカル優先）。
  - **波及の核**: `buildUsers()`(viewer.js:2000) の集約キーを alias 解決後 id にする1点改修＋`predOf` user(L2241) を集合一致化で、投稿数集計/`user` フィルタ/ジャンプ/カード描画がほぼ自動波及。`buildUsers` キャッシュは名寄せ変更で bust。
  - **手動UI**: 投稿者インスペクタ＋カード右クリック「同一人物にする/解除」＋投稿者ピッカー（`renderTagPicker` 流用）＋確認＋Undo（`pushUndo` に `poster-alias`）。D&D不採用（グリッド並び替えで誤操作が破壊的）。
  - **自動候補**: 決定的・説明可能ルールの重み合算（@ハンドル完全一致＞displayName 正規化一致＞類似）。根拠明示・確認で確定・薄いうちは沈黙（共起ヒントと同思想）。アバター知覚ハッシュ／プロフィールリンク相互参照（サイドカー保存の有無要確認）は段階③。
  - **段階**: ①基盤＋手動＋全波及 → ②自動候補 → ③候補強化/確認キュー。**要判断**: UI 置き場（推奨インスペクタ）／自動候補の強さ（強い手掛かりのみ推奨）／タグ・フォルダは「個別保存・合算表示（非破壊）」か物理マージか／primary 決め方／粒度（person 固定推奨）。
- **投稿者モードのサイドバー**: ライブラリと同じ「行＋フライアウト」方式へ統一済み（プラットフォーム/タグ/作品/キャラ/インスタンス/日付/フォルダ/ワークスペース＋件数バッジ・段階的開示）。表示切替（card/tile/list・タイルはアバター主役）・モード切替トグルのサイドバー常設・前回モード復元・フォルダ管理モーダル共用化も実装済み（git 参照）。**残**: 必要ならフォロワー数順など（X/pixivは公開APIにフォロワー無し＝大半で欠落するため未採用）。
- **投稿者モード→投稿一覧の新規タブ**: 現状はダブルクリックで**同じタブ**を投稿モードに切替＋その投稿者の `user` フィルタ（スイッチ切替想定に沿う）。**残**: 別タブで開く選択肢（ユーザーフィルタと投稿/画像フィルタの両立）を出すか。新規タブ案はスイッチ切替想定と整合が取りにくく、要設計のまま。
- **戻る/進むボタンの遷移対象にライブラリ↔投稿者モードの切替も含めたい（要設計）**: 現状の戻る/進む（履歴ナビ）は browseMode（ライブラリ/投稿者）の切替を履歴に積まない。モード切替も遷移対象にして戻る/進むで行き来できるように。論点＝履歴に積む単位（フィルタ変更との粒度整合）。
- ❌ 不採用（再提案しない）: 「カード＝インスペクタ全情報」取り下げ済み。

## その他
- **複数ウィンドウの許可**: 同時に複数のアプリウィンドウを開けるように（現状の単一ウィンドウ制約を緩める）。
- **重複保存の警告（コピー/置換/スキップ）**: 同じ投稿URLが既にライブラリにある状態で再保存（再キャプチャ/ドラッグ/取込）したら、止めて「両方残す/置き換える/スキップ」を選ばせる（ファイル重複ダイアログ式）。現状は captureId 単位で無条件に別ファイル保存＝同じ画像が重複し×Nが増える（実例: 6/14 の bluesky 投稿はキャプチャ＋ドラッグの二重保存で×2）。**設計調査済み（2026-06-21）**: 判定キーは既存 `postKeyOf(url)`(viewer.js:2056) の正規化プラットフォームキー（クエリ/フラグメント差を吸収・x⇄twitter 統合済み）を共有モジュールに切り出し renderer/main 共用。照会先はレンダラ保持の `allPosts`（url→captureId 逆引き Map を1本足すだけ・新索引不要・コンテンツハッシュは不採用＝スクショは毎回バイト差）。**警告位置はアプリ取込時に寄せる**＝拡張キャプチャは従来どおり一旦保存（write-once 維持）、アプリが delta 受信フックで同一キーの先客を検出→アプリ内で後追い重複解決ダイアログ。これで全キャプチャ経路（クリック/ドラッグ）を1箇所でカバー・ブリッジ無改造。3択は既存 `delete-post`(main.js:648・サイドカー＋コンパニオン＋索引整合を一手に)を再利用（置換=旧をソフト削除/スキップ=新を取消/両方=現状）。注: `import-images`(ローカル画像取込)は url=null＝重複対象外。**段階**: Phase0 `postKeyOf` 共有化→Phase1 `import-posts`(同期経路)を生URL一致から正規化＋3択化（UX 試作）→Phase2 キャプチャ経路の後追い検出ダイアログ→Phase3 既存重複の遡及掃除UI。**要判断**: 警告タイミング（後追い＝即時性犠牲 vs 拡張バナーで保存前＝重い）／判定キーの厳密さ（null 同士は重複扱いせず安全側で可か）／「置換」で旧のタグ/フォルダを新へ引き継ぐか／遡及掃除をやるか。
- コマンドパレット（Cmd+K 的＝フィルタ/操作/タブ移動の検索式ランチャ）。
- **検索バーの対象コントロール（詳細検索画面）**: 現状は検索ボックスの「あいまい/通常」切替だけで、**何を対象に検索するか**（本文/タイトル/投稿者/タグ/ハッシュタグ/期間/プラットフォーム…）はユーザーが選べない（固定で横断）。X や Outlook の高度な検索のように、**ボックスやプルダウンが並ぶ詳細検索パネル**を開けるようにして、フィールドごとに条件を指定できるように。要設計: 既存のクエリビルダー（改訂③＝ドラッグ式の条件ツリー）との役割分担＝**フォーム型UIとツリー型UIの整合**（同じ条件モデルを別の入口で編集する形にできるか）。コマンドパレット（操作/タブ移動ランチャ）／本文の全文検索（下記）とも別物。**設計調査済み（2026-06-21）**: 推奨＝**統合する**（フォームを別モデルにしない）。詳細検索フォームは既存 `queryTree`(viewer.js:1625-・ブール完全木) を編集する第3の入口にし、評価 `predOf`/`evalNode`(L2234-2277) は無改修・条件追加は既存共通API `addFilter`(L740) に乗せる（サイドバー行/日付/反応ポップと同じレール＝既に「2入口1モデル」）。**住み分け**＝フォーム＝床（ルート直下ANDの単純形・全フィールドが見える表）／ツリー（バー）＝天井（OR・括弧・否定・入れ子）。複雑な木のときフォームは読み取り表示（改訂②の平坦/複雑出し分けの再来）。対象は `predOf` が既に評価する約12軸から主要7軸を初手に。パネルは日付/反応ポップ(`qf-popover`)の大型ガラス版。**最重要論点**: 本文検索は現状 `queryTree` の外（固定横断 haystack・viewer.js:2201-）＝「対象を選べない」の正体。新 type `text`(field:body/title/any) として木の葉に格上げするか（要件を完全に満たすが影響大＝段階3）。段階1=フォーム枠＋既存フィールド転記／段階2=平坦複雑の出し分け＋往復／段階3=本文の木化。動的コレクション（保存検索・実装済み）は同じ `queryTree` を保存する層。
- **本文の全文検索（残＝専用UX）**: 検索の中身（本文/タイトル/pixivキャプション）マッチは**実装済み**＝検索ボックスが `p.text`/`p.title`/`screenName`/`displayName`/`tags` を通常・あいまい両方で横断（viewer.js のフィルタ・pixivキャプションは `metadata.js` で `rec.text` に格納済み）。OCR/AI不要でローカル完結＝オフライン方針と無衝突・競合（Karakeep/Eagle）より別格に低コスト。**残**: 現状は「現タブの投稿を絞り込む」クイック検索。ライブラリ全体を横断する**専用の全文検索体験**（全タブ越え・結果一覧・本文ハイライト/該当箇所ジャンプ等）を出すかは要設計。※コマンドパレット（操作/タブ移動ランチャ）とは別物。
- 開発共通ルールを親フォルダの CLAUDE.md に書き出し→重複削除（上位集約）。
- 拡張の実機E2E拡充: 特に **X＝要ログインで未自動化**（puppeteer は bot検出で弾かれる）。`e2e-capture-test.js`（全PF PASS・X除外）の延長で X を認証済みプロファイル/Claude in Chrome で。手動X残テスト（A-1系）もここで。
- **実機キャプチャの最終目視確認（残）**: native-host のポインタ復旧／共有 `resolveSaveFolder` 修正（commit 6ead0a9）は実 `%APPDATA%\Corpus` に deploy 済みで、**Chrome 無しの end-to-end は検証済み**（deploy 済み `bridge.js` を `require` し `handleSave` に合成JPEGを渡すと `C:\Users\apricot\local\corpus-library` に jpg+json を再現／詳細はメモリ `corpus-library-loss-incident`）。残るは**実ブラウザ経由の最終確認のみ**＝実機 Chrome で1件キャプチャ（Alt+S またはドラッグ）し、保存先に `.jpg`+`.json` が落ちるのを目視。リモート接続では対話キャプチャ不可＝実機で実施。
- **ロゴ／アイコンの小サイズ対応（特に拡張アイコンが潰れる）**: 現行ロゴ `{ ♡ }` は線画3本（波括弧2＋アウトラインのハート・stroke 2.8・ink＋sky の2色・viewBox `7 8 58 40` の横長）。大サイズ向きで、16/32px（拡張ツールバー/ファビコン）だと①横長 viewBox が正方アイコン枠でレターボックスされ実効サイズが縮む ②細ストロークがサブピクセル化して滲む/消える ③2色が判別不能、で潰れる。**ユーザー観察（正しい）＝世のアイコンが塗り・面主体なのは小サイズで生き残るため**（Apple HIG / Material / Fluent: app・favicon は塗り・マス・正方フィル・要素最小が定石）。**方向＝アイコンとワードマークを分離**: 線画 `{ ♡ }` は banner/README/ワードマーク（大サイズ）に温存し、app＋拡張用に**塗り主体の正方マーク**を別途用意（丸角タイル＋塗りのハート/波括弧・要素1〜2・太い形・正方 viewBox・枠の8割を埋める・最小色／「16px で読めるか」を基準）。`assets/icon-{light,dark}.svg` ＋ `extension/icons/icon{16,32,48,128,256}.png` を再出力、`logo-builder.html` を塗り版に拡張も検討。DESIGN.md「ブランド／ロゴ」の意味三位一体（`{ }`＝サイドカー・♡＝収集動機）は維持＝線画は捨てずアイコンだけ塗り派生にする。**2026-06-21 の結論＝塗りマーク方向は不採用・ロゴを根本から見直す（ユーザー判断）**: 塗り主体の正方マーク2案（`assets/icon-mark-{a,b}-{light,dark}.svg`）を試作し make-icons.js も塗り版に刷新したが、**この方向は採用しない**。試作SVGと make-icons.js の塗り版はリポに残置（未採用・**PNG 未再生成＝`extension/icons/*.png` は現行の線画のまま**・`assets/icon-{light,dark}.svg` も線画のまま）。
  - **新タスク＝ロゴ根本見直し（未着手・次は設計から）**: 塗りマーク化という前提に縛られず、ブランド/アイコンの方向そのものを設計から再検討する。引き継ぐ要件＝小サイズ（16/32px）で読める・正方・面/マス主体（Apple HIG/Material/Fluent の定石）。維持する制約＝意味の三位一体（`{ }`＝サイドカー・♡＝収集動機の分離禁止）とワードマーク（線画 `{ ♡ }`・Plex Mono）。塗りマーク2案は「塗りマーク方向の参考」として残すが採用案ではない。
- （個人作業）既存ライブラリのリンク→拡張で再キャプチャ（画像/メタ欠損分。backfill はメタのみ）。

（Electron移行・サイドカー保存・原寸メディアDL/表示・ハッシュタグ/インスタンス/ユーザー一覧タブ・Mastodon対応・モノトーン/ガラス材質系・コントラスト計算検証は実装済み＝CLAUDE.md「構成」「ビューア機能」を参照。ユーザー一覧タブのフォロー数/作成日付与は未実装。）

## タグ付け／整理（クラスタ）

- **作品/キャラを独立カテゴリ（第一級概念）に（設計確定 2026-06-20／Phase 2 ①②＋共起候補＋種別リネーム＋同名解決 実装済み）**: 作品（copyright）とキャラ（character）を**タグのカテゴリ**に格上げし、サイドバーも独立セクションへ。種類が爆発的に増えるので埋もれさせない（前例＝Eagle/Pixiv/danbooru）。
  - **データモデル＝用語帳方式（Phase 2 ① 実装済み）**: `tags[]` はフラットのまま、別ファイルに「タグ→種別（作品/キャラ/一般）」を登録。種別は**タグの属性**（投稿の属性でない）＝**投稿約7600枚の移行ゼロ・分類するのは異なりタグ数百だけ**。フラットなラベル分けは既存タググループに委譲（被らせない）。
    - **実装内容**: `tag-types.json`＝`{ types: { "<タグ>": "work"|"character" } }`（未登録タグは暗黙で一般）。`get/set-tag-types` IPC、`INTERNAL_FILES`/clear-all保持/import の union merge（衝突時はローカル分類優先＝`mergeTagTypes`）に登録。**入口**＝スタンプのタグパレットで**チップ右クリック→「種別: 作品/キャラ/一般」**（既存のグループ割当と同じタグ管理面・地味なオプション＝段階的開示）。種別付きチップは小さなカテゴリドット（作品=紫/キャラ=緑＝danbooru 慣習）＋ツールチップで反映。テスト＝`test-tag-types.js`/`test-app-tagtypes.js`。
    - **格上げ表示（Phase 2 ② 実装済み 2026-06-20）**: **サイドバー**と**タグ付与画面（スタンプのパレット）**の両方で作品/キャラを独立セクション化。種別を1つでも付けたタグがあれば**自動で咲き**、無ければ出ない（段階的開示）。種別タグは**グループ/未分類から外す**＝種別が第一級カテゴリ（danbooru 式に1タグ1箇所・被らせない）。
      - サイドバー: フィルタ行に作品/キャラの独立行（タグ行の上・booru 慣習で作品→キャラ→タグ）。行クリックで既存フライアウトに**その種別のタグだけ**を並べ、選ぶと通常のタグ絞り込み（`type:'tag'`）＝ピン留め可。行頭に種別ドット（作品=紫/キャラ=緑）。件数バッジは種別で分割（タグ行は一般タグのみ＝二重計上しない）。
      - パレット: `groupedTagVocab` が作品/キャラを最上段のセクションに出し、グループ（人物/角度/形式）・未分類は一般タグのみ。右クリックで種別を付けると即座に該当セクションへ移動。
      - 実装＝viewer.js `groupedTagVocab`/`qfValues`/`updateKindRows`/`updateSidebarTagGroups`/`renderFilterBadges`＋index.html の2行。
    - **共起候補（作品→キャラ／実装済み 2026-06-20）**: カード編集モーダルのタグピッカーで、そのカードに作品種別タグがあり検索が空のとき、その作品と過去に共起したキャラ種別タグを最上段に候補として出す（recognition over recall・上位8・未付与のみ）。決定的/確認付き＝勝手に付けずクリック付与・全語彙は下に残す・tooltipに「N件で共起」と根拠・共起ゼロなら沈黙。実装＝viewer.js `charCandidatesFor`/`renderEditPicker`＋i18n。**現況（2026-06-20 実測）**: 作品種別17・キャラ種別2・作品×キャラ共起0件＝今は候補が出ない（Eagle側でもキャラ付与は開始直後・今後増える前提＝運用しつつFB）。
  - **段階的開示（汎用ツールの筋を守る）**: 種別を1つも付けなければ専用セクションもフィールドも出ない＝**ただ投稿を保存する人には痕跡ゼロ**（Corpus はイラスト整理専用ではない）。分類の入口は既存タグ管理内の地味なオプション（タグ右クリック「種別：作品/キャラ」、今のグループ割当と同じ場所＝鶏卵回避）、結果のリッチUIは使うと咲く。出し方は (a) 種別を付けたら自動で現れる（設定ゼロ・採用）方向。
  - **A=見せる機能／B=裏方**: A（格上げ＝カテゴリで棚分け）はユーザーに見せる本体。B（作品⊃キャラの**親子構造**）は“親子タグ機能”として**見せない**（価値が伝わりにくい）。②**作品→キャラ候補**（作品を選ぶ→共起するキャラ種別だけ候補に絞る＝recognition over recall）は A の種別＋既存付与の**共起から導出**＝新概念なし・確認付き。キャラ→作品の自動付与（インプリケーション）は共起からの“そっと提案”止まり・v1は任意。
  - **手動の親子紐付けUIは作らない（不要・確定）**: 作品とキャラを同じ投稿に付ける行為が紐付けそのもの＝1枚でも共起すれば次から候補に出る。前に保険で挙げた「小さな補正口」も不要。キャラだけ付けて作品を欠く投稿が続く穴は上のインプリケーション（そっと提案）で埋める。
  - **同名キャラ（別作品）の解決**: タグは文字列1個＝グローバルに1タグなので、「アリス」が東方/SAO 両方に居ると絞り込みも共起も両作品が混ざる穴がある。**衝突時だけ**キャラ名に作品を添えて区別（danbooru 方式・**freeform 文字列** `アリス（SAO）`／検知時に補完）。大半のキャラは衝突しない。**システムが検知して促せる**: 新規付与時、その投稿の作品が過去「アリス」の共起作品と違えば「別作品の同名キャラ？ 区別する？」と提示（ここでも共起を活用）＝共起はきれいなまま・手動紐付け不要・用語帳方式のまま。構造化（キャラ実体＋作品フィールド）は避けたい親子リンクモデルに戻るので不採用。
  - **共起が効く理由**: カテゴリ定義が (a) 関係に参加するタグ (b) 親方向 の2つの推測を消す＝残るは「どのキャラがどの作品か」だけで高精度（キャラはほぼ1作品としか共起しない）。
  - **種別ペアは改名可**: 既定「作品/キャラ」だがリネーム可でイラスト以外（シリーズ⊃話数・ブランド⊃商品 等）にも流用可。
  - **不採用**: ML自動タガー(WD14)・外部逆引き(SauceNAO/IQDB)はユーザー却下。なお Phase 1（ソースタグを一級市民化＝検索対象化＋「採用」でハッシュタグ→tags 格上げ）は実装済み（git 参照）。※投稿者まわりの「作品タググループを第一級概念へ」もここに集約。
- **タグを複数グループに属させることを可能にすべきか検討（未着手）**: 現状タググループは実質**1タグ1グループ**（どのグループにも無いタグ＝未分類。`groupedTagVocab`/サイドバーのサブ行は「グループ済み or 未分類」の二分で組まれている）。1タグが複数の観点に属したいケース（例: あるタグが「構図」でも「主題」でもある）に**多重所属**を許すべきか。許す場合の影響＝サイドバーのグループ別表示・件数集計の重複、パレット/ピッカーで同タグが複数セクションに出る、未分類の定義が変わる。種別（用語帳・1タグ1種別の排他レイヤー）とは別軸の話。前例＝danbooru はタグのカテゴリ1つ（排他）／Eagle・Notion はタグの多重付与可。判断軸＝多観点整理の柔軟さが、UIの重複表示・集計の複雑化に見合うか。
- **関連タグ提案（共起・確認付き／未着手）**: 全タグの同時出現から「X を付けた→Y もよく一緒」を**弱いヒント**で出す汎用版（上記②の作品/キャラ・スコープ付き共起とは別の、二次的機能）。**思想＝原則の精緻化**: ユーザーが却下したのは不透明・低精度・自動適用のレコメンド（ML/外部）。共起は**自分の付与の鏡で決定的・説明可能（共起回数を示せる）・押し付けずフィルター/並べ替え・常に確認で確定**＝原則の“いい側”。守る条件: ①押し付けない（候補を絞る/上に出すだけ・全語彙は残す）②なぜ出たか説明可能 ③勝手に付けない ④信頼度を段階化（スコープ付き＝強・全タグ＝弱）⑤データが薄いうちは出さない。カテゴリ無しの全タグ共起はノイズ＋親方向が推測になるので弱ヒント止まり。
- **タグ付けの手間を軽減 or 根本解決（注力テーマ・案を検討中 2026-06-19）**: booru 型イラストアーカイブなので「作品/キャラを当てる」既存資産が効く。案（積み上げ式）:
  - ① **ソースタグ取り込み（決定的・即効）**: pixiv 作品タグ（API に作品名/キャラ名）を最大の鉱脈に。X/Misskey ハッシュタグも候補。投稿者を強い prior に（絵師→だいたい[作品X]を一括→例外だけ直す）。
  - ② **ローカルML自動タガー（根本解決の本命）**: WD14 / wd-eva02（danbooru学習・ONNX・**ローカル**＝画像を外に出さない）が character/copyright/general を信頼度付きで出力。高=自動候補・中=確認・低=捨てる。前例＝Hydrus Network 内蔵。
  - ③ **逆引き（任意・外部）**: SauceNAO/IQDB→danbooru の人手キュレーション済みタグ。正確だが外部API（レート/画像送信）＝opt-in。
  - ④ **確認キューUI**: 候補を1タップ承認（前例＝Google Photos の顔まとめ確認）。「N枚が[キャラA]と推定→まとめて承認/例外だけ外す」。
  - **設計判断**: DESIGN/docs の「レコメンド/自動候補は不採用＝決定的・ユーザー主導」を見直す話。折衷＝①は決定的でOK／②は**確認ゲート付き**で最終決定は人＝原則と両立、と整理。要ユーザー判断。
  - **実測（2026-06-19・本ライブラリ集計）**: 全 **9048 件中 Eagle 移行が 9012 件（99.6%）**＝booru 型イラストが主体（SNS 取込は X 863 等のごく一部・原寸 media 有りは 6 件のみ）。**タグ付きは 15.6%（1406 件）＝約 7600 枚が無タグ**。既存タグは**作画資料寄り**（ポージング547・髪500・ななめ251・塗り218・正面・座り・俯瞰・タレ目…）＋作品名（ブルアカ340・アークナイツ121・スターレイル114）＋NSFW90 で、**すべて日本語**。`title` はキャラ/作品名でなく**元ファイル名が大半**（pbs・pixiv連番・Gyazo名）＝当てにならない。→ **① ソースタグ取込はこのライブラリでは無力**（pixiv 13 件・Eagle 移行は URL 無し）。無タグ約7600枚を埋める実効レバーは **②ローカルML(WD14) か ④手動UX改善に絞られる**。新課題＝WD14 は**英 danbooru タグ出力**でユーザー語彙は日本語＝**語彙マッピング**（作品名/キャラ/一般タグの対訳）が要る。
  - **次の一手（WD14 調査済み 2026-06-21）**: ローカル推論は技術的に現実的＝`SmilingWolf/wd-eva02-large-tagger-v3`（Apache-2.0・model.onnx 約1.26GB＋selected_tags.csv／軽量版 wd-vit-v3 は数百MB）。Node 完結は `onnxruntime-node`（Electron 対応・CPU は追加依存ゼロ・electron-builder で `asarUnpack` 必須・GPU は DirectML/CUDA EP）。前処理＝448×448/RGB→BGR/白パディング。閾値はカテゴリ別（general 0.35・character 0.85）＝高/中/低運用に直結。**語彙マッピングは既製辞書あり**＝`isek-ai/danbooru-wiki-2024`（CC-BY-SA-4.0・45.7MB・`other_names` に日本語別名）で**作品/キャラは高カバレッジ**（corpus は作品/キャラを種別=用語帳化済み＝流し込み先が在る＝最大の効きどころ）。一般タグは粒度が独自で機械対訳が不完全＝英タグのまま付与＋頻出語だけ手動補正＋確認で蓄積（完璧は目指さない／割り切るなら general は CLIP 検索に委ねる）。**確認ゲート付き運用は却下理由（不透明・低精度・自動適用）を1つも踏まない**＝原則の精緻化（64行の整理と一致）。CLIP 意味検索は競合でなく補完（曖昧視覚検索／固有名詞は WD14 が圧倒）。**要判断**: (a) 代表100枚での小スパイク（推論時間/作品キャラ命中率/辞書命中率の実測）を実施するか (b) Python sidecar 許容か Node 完結か (c) 原則の確認ゲート付き見直し可否 (d) 英日マッピングの不完全さ（作品キャラ自動・general 割り切り）を許容するか。出典は調査レポート（セッション履歴）。
  - **別解**: CLIP意味検索＋OCR＋本文検索で「整理用の検索」が足りるなら、タグ付け自体を減らせる（本文検索は実装済み＝上「その他」参照）。

## UX再設計の指摘（2026-06-20 フィードバック・要整理＝次は設計から）

このセッションで出た UX 指摘群。相互依存が強いので実装前に設計を固める。インスペクタ・タグ編集のインライン化（中央モーダル→右パネル・即時保存）と、モーダル時の背景スクロール/タイトルバー暗転は実装済み（git）。

### ナビ/検索UI
- **クエリビルダーがアドレスバーに見える＝初見が「クリックして入力」を試す（未決・要判断／2026-06-21 提起）**: 初見の人はクエリビルダー帯をブラウザのアドレスバーと誤認し、クリック→テキスト入力を試す。対応の二択（ユーザー提起・**僕の意見＋ユーザー判断待ち**）: **(a)** 「ここには直接入力できません／フィルタは◯◯から」のヒントを出す（軽い・現行のフィルタ追加導線へ誘導）／ **(b)** ブラウザ風に**テキスト入力＋候補サジェストで直接フィルタを投入**できるようにする（誤認を機能に変える＝アドレスバー的オムニ入力。既存 `queryTree`/`addFilter` に乗せる）。bは「検索バーの対象コントロール（詳細検索画面）」「コマンドパレット」「本文の全文検索」と接近＝設計を寄せられるか。次セッションで方針決め。

## リリース準備
- **配布パッケージング**（electron-builder, win/nsis）
- アプリ初回起動時に**拡張インストールのガイド**（ストア公開後）。未インストール/未接続を検知して案内。

### 0. Corpus リリース前準備（2026-06-11 時点の決定）

**ユーザー決定済み**: リリース時にリポジトリ（apricot-cake/corpus）を **public 化する**。収益化の予定なし。ライセンスは未決定だが**緩い系でよい**。コード署名は**やる方針**。それまでは private 厳守。

### 1. 自動アップデート配信（実装は未着手・リリース準備時に）

- **electron-updater + 本リポの GitHub Releases**（public化するのでリリース専用リポは不要になる）。
- 実装手順: ①`app/package.json` に electron-updater 追加 ②electron-builder の `publish: [{ provider: 'github', owner: 'apricot-cake', repo: 'corpus' }]` ③`app/main.js` に `autoUpdater.checkForUpdatesAndNotify()`（起動時＋任意で定期）④リリースは version bump → `npm run dist -- --publish always`（GH_TOKEN 必要）。
- NSIS + blockmap で差分アップデート。latest.yml の sha512 で整合検証。
- 拡張側は Chrome Web Store に出せばブラウザが自動更新（アプリとは別系統）。

### 2. ライセンス

- 緩い系 → **MIT 推奨**（収益化なし・依存も問題なし）。リリース時に LICENSE ファイル＋ app/package.json の `license` フィールド追加。

### 3. コード署名（やる方針）

- 効果: SmartScreen 初回警告の解消（評判蓄積）、アップデート改ざん耐性、AV誤検知の低減。
- **候補（OSS・無収益前提）**:
  - **SignPath Foundation（OSS無料）**: public リポ＋CI 連携が条件。リポ public 化と相性が良く第一候補。審査あり。
  - **Azure Trusted Signing**: 約$9.99/月。個人開発者の本人検証に対応（地域条件は申請時要確認）。electron-builder が統合サポート。
  - **Certum Open Source Code Signing**: 年€70前後＋カード/リーダー初回費用。OSS向けの定番安価OV。
- electron-builder の win.sign / signtool 設定が必要。NSIS ビルドの開発者モード要件（winCodeSign symlink）は docs/build.md 記載済み。